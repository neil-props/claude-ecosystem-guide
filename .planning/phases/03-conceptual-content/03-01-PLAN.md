---
phase: 03-conceptual-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/mcp.md
  - content/skills.md
  - content/plugins.md
  - content/hooks.md
  - content/agents.md
autonomous: true
requirements:
  - CONC-01
  - CONC-02
  - CONC-03
  - CONC-04
  - CONC-05

must_haves:
  truths:
    - "MCP page has tab UI with Concept/How-To/Reference tabs"
    - "MCP conceptual overview covers all three transport types (stdio, SSE, streamable HTTP)"
    - "Skills page explains frontmatter fields and when to use skills vs agents/hooks/commands"
    - "Plugins page explains how plugins bundle skills and distribution methods"
    - "Hooks page frames hooks as deterministic automation outside the agentic loop"
    - "Agents page explains custom agent files and when to use agents vs skills"
    - "All 5 pages have placeholder How-To and Reference tabs with coming-soon content"
  artifacts:
    - path: "content/mcp.md"
      provides: "MCP conceptual overview with tab UI"
      contains: "tab-panel"
    - path: "content/skills.md"
      provides: "Skills conceptual overview with tab UI"
      contains: "tab-panel"
    - path: "content/plugins.md"
      provides: "Plugins conceptual overview with tab UI"
      contains: "tab-panel"
    - path: "content/hooks.md"
      provides: "Hooks conceptual overview with tab UI"
      contains: "tab-panel"
    - path: "content/agents.md"
      provides: "Agents conceptual overview with tab UI"
      contains: "tab-panel"
  key_links:
    - from: "content/mcp.md"
      to: "docs/topics/mcp.html"
      via: "build.js markdown-to-HTML pipeline"
      pattern: "node build/build.js"
    - from: "content/skills.md"
      to: "docs/topics/skills.html"
      via: "build.js markdown-to-HTML pipeline"
      pattern: "node build/build.js"
---

<objective>
Restructure the 5 most complex topic files (MCP, Skills, Plugins, Hooks, Agents) with tab-based content layer UI and enhance their conceptual overviews.

Purpose: These are the core extension points users need to understand. Each page gets a Concept/How-To/Reference tab structure, with the Concept tab containing an enhanced overview of what the feature is, when to use it, and how it fits in the ecosystem. How-To and Reference tabs get placeholder content pointing to Phase 4.

Output: 5 restructured content files with tab UI and deepened conceptual narratives.
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conceptual-content/03-RESEARCH.md

@content/mcp.md
@content/skills.md
@content/plugins.md
@content/hooks.md
@content/agents.md
@content/index.md (lines 104-119 -- working tab markup reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure MCP, Skills, and Plugins with tab UI and enhanced concepts</name>
  <files>content/mcp.md, content/skills.md, content/plugins.md</files>
  <action>
For each of the 3 files (mcp.md, skills.md, plugins.md), restructure to use the tab-based content layer UI:

**Structure for each file:**
1. Keep the existing YAML frontmatter unchanged
2. Keep the H1 page title above the tabs
3. Wrap all content in the tab structure:

```html
<div class="tabs">
  <div class="tab-group">
    <button class="tab-btn active" data-tab="concept">Concept</button>
    <button class="tab-btn" data-tab="howto">How-To</button>
    <button class="tab-btn" data-tab="reference">Reference</button>
  </div>
  <div class="tab-panel active" data-tab-panel="concept">

[Concept content here -- BLANK LINE after div tag is critical for markdown rendering]

  </div>
  <div class="tab-panel" data-tab-panel="howto">

[Placeholder content]

  </div>
  <div class="tab-panel" data-tab-panel="reference">

[Placeholder content]

  </div>
</div>
```

**Concept tab content structure** (consistent across all files):
1. **Overview** (##) -- Opening paragraph: what it is in one sentence + mental model. Then ecosystem context (which Claude surfaces support it, who maintains it). Then core capabilities as 3-5 bullets.
2. **How It Works** (##) -- Technical explanation with existing tables/diagrams. Keep existing subsections.
3. **When to Use [Feature]** (##) -- "Use X when..." and "Don't use X when..." guidance. How it relates to other extension points.
4. **Best Practices** (##) -- Move existing best practices content here.
5. **Common Questions** (##) -- Move existing FAQ content here.

**Per-file content enhancements:**

**mcp.md (CONC-01):**
- Ensure Overview covers all 3 transport types: stdio (local processes), SSE (legacy remote), streamable HTTP (current recommended remote)
- Position streamable HTTP as the current standard, SSE as legacy but still functional
- Mention AAIF, vendor-neutral nature, 97M+ monthly SDK downloads
- "When to Use MCP" section: use for external tool/data integration; don't use when a simple skill would suffice

**skills.md (CONC-02):**
- Enhance Overview with clear "what is a skill" narrative -- markdown files with YAML frontmatter that teach Claude new abilities
- Ensure frontmatter fields are explained conceptually (not as a spec -- that's Phase 4 Reference tab)
- Add "When to Use Skills" with explicit comparison: skills vs agents (skills are reusable instructions, agents are isolated contexts), skills vs hooks (skills are on-demand, hooks are automatic), skills vs commands (skills replace legacy commands)

**plugins.md (CONC-03):**
- Enhance Overview with "plugins bundle skills for distribution" narrative
- Cover distribution methods: npm packages, git repos, manual installation
- Add "When to Use Plugins" -- when you have multiple related skills to distribute as a package vs sharing individual skill files

**How-To tab placeholder** (same for all 3 files):
```markdown
## How-To Guides

> [!INFO]
> Step-by-step guides for [topic] are coming in Phase 4.

Planned guides:
- [relevant HOWT-* items from REQUIREMENTS.md as bullet list] -- _coming soon_
```

**Reference tab placeholder** (same for all 3 files):
```markdown
## Technical Reference

> [!INFO]
> Detailed reference specs for [topic] are coming in Phase 4.

Planned references:
- [relevant REFR-* items from REQUIREMENTS.md as bullet list] -- _coming soon_
```

**Critical:** Always leave a blank line after opening `<div>` tags and before closing `</div>` tags for markdown rendering inside HTML blocks. Do NOT nest `.tabs` containers. Keep all headings inside tabs at h2 (##) level.

Place any "Related" section OUTSIDE the tab structure as a page-level footer after the closing `</div>` of the tabs container.
  </action>
  <verify>
    <automated>cd /Users/sharode/Desktop/claude-ecosystem-guide && grep -c "tab-panel" content/mcp.md content/skills.md content/plugins.md && grep -c "stdio\|streamable HTTP\|SSE" content/mcp.md && grep -c "frontmatter\|vs.*agent\|vs.*hook" content/skills.md && grep -c "bundle\|distribution\|install" content/plugins.md && node build/build.js</automated>
  </verify>
  <done>
- mcp.md has tab UI with Concept tab covering all 3 transport types, ecosystem context, and when-to-use guidance
- skills.md has tab UI with Concept tab explaining frontmatter and comparison to agents/hooks/commands
- plugins.md has tab UI with Concept tab explaining bundling and distribution
- All 3 files have How-To and Reference placeholder tabs with specific planned items from requirements
- Build succeeds with restructured content
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure Hooks and Agents with tab UI and enhanced concepts</name>
  <files>content/hooks.md, content/agents.md</files>
  <action>
Apply the same tab restructuring pattern from Task 1 to hooks.md and agents.md.

**hooks.md (CONC-04):**
- Enhance Overview to frame hooks as "deterministic automation outside the agentic loop" -- code that runs automatically at specific lifecycle events without Claude deciding whether to run it
- Keep the comprehensive event table (PreToolUse, PostToolUse, Notification, Stop, SubagentStop) in the How It Works section
- Add "When to Use Hooks" with clear guidance: use hooks for guardrails, compliance checks, automatic formatting, notifications; don't use hooks when you need Claude's judgment (use skills instead)
- Mention the matchers system (tool_name, file_paths patterns) conceptually

**agents.md (CONC-05):**
- Enhance Overview to explain custom agents as "isolated Claude instances with specific instructions and tool access"
- Cover both built-in agents (subagents launched automatically) and custom agent files
- Add "When to Use Agents" with comparison: agents vs skills (agents get isolated context and can be invoked via CLI, skills are instructions loaded into the main session), agents vs direct prompting (agents provide reproducible, shareable workflows)
- Explain the `--agent` flag and agent file format conceptually (detail in Phase 4 Reference tab)

**Same placeholder structure** for How-To and Reference tabs as Task 1, using relevant HOWT-* and REFR-* items.

**Same critical rules:** Blank lines after `<div>` tags, h2 headings inside tabs, "Related" section outside tabs.
  </action>
  <verify>
    <automated>cd /Users/sharode/Desktop/claude-ecosystem-guide && grep -c "tab-panel" content/hooks.md content/agents.md && grep -c "lifecycle\|event\|deterministic" content/hooks.md && grep -c "agent file\|custom agent\|isolated context" content/agents.md && node build/build.js</automated>
  </verify>
  <done>
- hooks.md has tab UI with Concept tab framing hooks as deterministic automation with lifecycle events
- agents.md has tab UI with Concept tab explaining custom agents, agent files, and when to use
- Both files have How-To and Reference placeholder tabs
- Build succeeds with all 5 Wave 1 files restructured
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `node build/build.js` succeeds without errors
2. `grep -l "tab-panel" content/mcp.md content/skills.md content/plugins.md content/hooks.md content/agents.md | wc -l` returns 5
3. `node --test build/test-build.js` passes
4. Each file has exactly 3 tab panels (concept, howto, reference)
</verification>

<success_criteria>
- All 5 topic files (MCP, Skills, Plugins, Hooks, Agents) restructured with Concept/How-To/Reference tab UI
- Each Concept tab has enhanced overview meeting its CONC-* requirement
- MCP covers all 3 transport types (CONC-01)
- Skills explains frontmatter and comparison guidance (CONC-02)
- Plugins explains bundling and distribution (CONC-03)
- Hooks frames deterministic automation with lifecycle events (CONC-04)
- Agents explains custom agents and when-to-use guidance (CONC-05)
- How-To and Reference tabs have meaningful placeholder content listing planned Phase 4 items
- Build pipeline produces valid HTML for all 5 pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-conceptual-content/03-01-SUMMARY.md`
</output>
