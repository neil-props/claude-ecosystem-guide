---
phase: 04-guides-and-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-howto-content.js
  - tests/test-reference-content.js
  - content/mcp.md
autonomous: true
requirements: [HOWT-01, HOWT-02, HOWT-03, HOWT-04, REFR-01]

must_haves:
  truths:
    - "MCP how-to tab has a complete stdio setup guide a user can follow start to finish"
    - "MCP how-to tab has a complete SSE/streamable HTTP setup guide"
    - "MCP how-to tab has an authentication configuration guide"
    - "MCP how-to tab has a build-your-own-server guide"
    - "MCP reference tab has the complete .mcp.json config schema with all fields documented"
    - "Test files exist that verify how-to and reference tabs contain real content (not placeholders)"
  artifacts:
    - path: "tests/test-howto-content.js"
      provides: "Spot-check tests for how-to tab content across all topic pages"
    - path: "tests/test-reference-content.js"
      provides: "Spot-check tests for reference tab content across all topic pages"
    - path: "content/mcp.md"
      provides: "MCP page with populated how-to and reference tabs"
      contains: "mcpServers"
  key_links:
    - from: "content/mcp.md"
      to: "docs/topics/mcp.html"
      via: "build.js markdown-to-HTML pipeline"
      pattern: "tab-panel.*howto"
---

<objective>
Create test scaffolds for how-to and reference content validation, then write complete MCP how-to guides (stdio, SSE, auth, build-your-own) and MCP config schema reference.

Purpose: MCP is the most complex extension point with 4 how-to guides and a detailed config schema. Completing it first establishes the content pattern for other plans. Test scaffolds enable automated verification for all Phase 4 plans.
Output: 2 test files + fully populated MCP how-to and reference tabs
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-guides-and-reference/04-RESEARCH.md
@.planning/phases/04-guides-and-reference/04-VALIDATION.md
@content/mcp.md

<interfaces>
<!-- Existing test pattern from tests/ directory -->
<!-- Tests use Node.js built-in test runner (node --test) -->
<!-- Build pipeline: node build/build.js converts content/*.md to docs/topics/*.html -->
<!-- Tab panels use: <div class="tab-panel" data-tab-panel="howto"> and data-tab-panel="reference"> -->
<!-- Existing how-to placeholders contain: "coming in Phase 4" and "coming soon" text -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffolds for how-to and reference content</name>
  <files>tests/test-howto-content.js, tests/test-reference-content.js</files>
  <action>
Create two test files using Node.js built-in test runner (matching existing test patterns in tests/):

**tests/test-howto-content.js:**
- Read built HTML files from docs/topics/ (run build first if needed)
- For each topic page that should have how-to content, verify:
  - The howto tab-panel exists and contains more than just placeholder text
  - Spot-check for key phrases indicating real guide content:
    - mcp.html: "Prerequisites", "Step 1", "Verify It Works" (for HOWT-01)
    - skills.html: "Prerequisites", "Step 1" (for HOWT-05)
    - plugins.html: "Prerequisites", "Step 1" (for HOWT-06)
    - hooks.html: "Prerequisites", "Step 1" (for HOWT-07)
    - agents.html: "Prerequisites", "Step 1" (for HOWT-08)
    - projects.html: "Prerequisites", "Step 1" (for HOWT-09)
    - memory.html: "Prerequisites", "Step 1" (for HOWT-10)
  - Verify no "coming in Phase 4" or "coming soon" placeholder text remains in howto panels
- Tests should initially fail (only mcp.html will pass after Task 2 in this plan)

**tests/test-reference-content.js:**
- Read built HTML files from docs/topics/
- For each topic page that should have reference content, verify:
  - The reference tab-panel exists and contains more than placeholder text
  - Spot-check for key phrases:
    - mcp.html: "Field", "Type", "Required" (table headers for schema docs)
    - skills.html: "Field", "Type" (for REFR-02)
    - plugins.html: "Field", "Type" (for REFR-03)
    - hooks.html: "Field", "Type" (for REFR-04)
    - agents.html: "Field", "Type" (for REFR-05)
    - settings.html: "Field", "Type" (for REFR-06)
    - projects.html: "CLAUDE.md" (for REFR-07)
  - Verify no "coming in Phase 4" placeholder text remains in reference panels

Follow the existing test file pattern (look at tests/test-content-completeness.js or tests/test-build.js for import style, describe/it structure).
  </action>
  <verify>
    <automated>node --test tests/test-howto-content.js 2>&1 | head -30; node --test tests/test-reference-content.js 2>&1 | head -30</automated>
  </verify>
  <done>Both test files exist and run without syntax errors. Tests fail for pages that still have placeholder content (expected at this point -- only MCP will be populated by Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Write MCP how-to guides and config schema reference</name>
  <files>content/mcp.md</files>
  <action>
Replace the placeholder content in mcp.md's howto and reference tab panels with complete content. The file currently has placeholders at lines ~198-223. Keep everything outside these tab panels unchanged.

**How-To tab (data-tab-panel="howto") -- replace placeholder with 4 guides:**

1. **How to Set Up an MCP Server (stdio)** (HOWT-01):
   - Prerequisites: Claude Code installed, Node.js for npx-based servers
   - Step 1: Add server via CLI: `claude mcp add github-server -- npx @modelcontextprotocol/server-github`
   - Step 2: Verify with `claude mcp list`
   - Step 3: Share with team via project `.mcp.json` file at repo root (show the JSON with command, args, env using `${GITHUB_TOKEN}` syntax)
   - Step 4: Verify in Claude Code session ("Ask Claude to use a GitHub tool")
   - Verify It Works section
   - Troubleshooting: server not appearing, env vars not resolving

2. **How to Set Up an MCP Server (SSE/Streamable HTTP)** (HOWT-02):
   - Prerequisites: Remote MCP server URL, auth token if needed
   - Step 1: Add remote server: `claude mcp add --transport http analytics-server https://mcp.example.com/analytics`
   - Step 2: Add with auth headers if needed (show header syntax)
   - Step 3: Verify connection
   - Note: Streamable HTTP is the current standard, replacing SSE

3. **How to Configure MCP Authentication** (HOWT-03):
   - Step 1: Environment variable expansion in `.mcp.json` using `${ENV_VAR}` syntax
   - Step 2: OAuth 2.0 flow for supported servers
   - Step 3: Custom auth headers for HTTP transport
   - Step 4: Managed MCP allowlists (`managed-mcp.json` with allowedMcpServers/deniedMcpServers)

4. **How to Build Your Own MCP Server** (HOWT-04):
   - Prerequisites: Node.js, TypeScript recommended
   - Step 1: Install SDK: `npm install @modelcontextprotocol/sdk`
   - Step 2: Create server file with a tool definition (show realistic example -- e.g., a "get-weather" tool with JSON schema input)
   - Step 3: Register locally: `claude mcp add my-server -- node ./my-server.js`
   - Step 4: Test in Claude Code session
   - Verify It Works section

All guides must use the template from research: Prerequisites, numbered Steps, Verify It Works, Troubleshooting. Code examples must be copy-pasteable with realistic values.

**Reference tab (data-tab-panel="reference") -- replace placeholder with REFR-01 schema:**

1. **.mcp.json Config Schema** (REFR-01):
   - File Location: `.mcp.json` at project root (project scope), `~/.claude/.mcp.json` (user scope)
   - 4 scopes: project, user, enterprise managed, enterprise denied
   - Complete JSON schema example showing all fields
   - Field Reference table: | Field | Type | Required | Default | Description |
     - command (string, required for stdio)
     - args (string[], optional)
     - env (object, optional -- supports `${VAR}` expansion)
     - type (string, optional -- "stdio" default, "http")
     - url (string, required for http type)
     - headers (object, optional for http)
   - managed-mcp.json schema (allowedMcpServers, deniedMcpServers arrays)
   - Validation rules
   - Minimal example + full example with multiple servers

Also migrate any detailed MCP config JSON currently in the Concept tab to the Reference tab if appropriate, leaving a lighter summary in Concept with "see the Reference tab for complete schema" pointer.

Build and verify: `node build/build.js && node --test tests/test-howto-content.js`
  </action>
  <verify>
    <automated>node build/build.js && node --test tests/test-build.js && node --test tests/test-howto-content.js 2>&1 | grep -E "mcp|pass|fail" | head -20</automated>
  </verify>
  <done>MCP how-to tab contains 4 complete guides (stdio setup, SSE setup, auth config, build-your-own). MCP reference tab contains complete .mcp.json schema with field reference table. No placeholder text remains in MCP how-to or reference tabs. Build succeeds. How-to content test passes for MCP page.</done>
</task>

</tasks>

<verification>
- `node build/build.js` succeeds with no errors
- `node --test tests/test-build.js` passes
- `node --test tests/test-howto-content.js` -- MCP tests pass, others expected to fail (placeholder still present)
- `node --test tests/test-reference-content.js` -- MCP tests pass, others expected to fail
- No "coming in Phase 4" or "coming soon" text in mcp.html howto/reference panels
</verification>

<success_criteria>
- MCP page has 4 how-to guides that are complete walkthroughs (prerequisites through verification)
- MCP page has .mcp.json config schema reference with field table
- Test scaffolds exist and can detect placeholder vs real content
- All code examples use realistic values (no lorem ipsum)
- Build pipeline produces valid HTML output
</success_criteria>

<output>
After completion, create `.planning/phases/04-guides-and-reference/04-01-SUMMARY.md`
</output>
