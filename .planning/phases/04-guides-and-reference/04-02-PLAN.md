---
phase: 04-guides-and-reference
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - content/skills.md
  - content/plugins.md
  - content/hooks.md
  - content/agents.md
autonomous: true
requirements: [HOWT-05, HOWT-06, HOWT-07, HOWT-08, REFR-02, REFR-03, REFR-04, REFR-05]

must_haves:
  truths:
    - "Skills how-to tab has a complete guide for creating a skill with correct frontmatter"
    - "Plugins how-to tab has a complete guide for creating a plugin that bundles skills"
    - "Hooks how-to tab has a complete guide for setting up hooks for lifecycle events"
    - "Agents how-to tab has a complete guide for creating a custom agent"
    - "Skills reference tab has the complete frontmatter spec with all 10 fields"
    - "Plugins reference tab has the complete plugin.json manifest spec"
    - "Hooks reference tab has the complete hook configuration spec with all 24 events"
    - "Agents reference tab has the complete agent file format spec with all 12 fields"
  artifacts:
    - path: "content/skills.md"
      provides: "Skills page with populated how-to and reference tabs"
      contains: "frontmatter"
    - path: "content/plugins.md"
      provides: "Plugins page with populated how-to and reference tabs"
      contains: "plugin.json"
    - path: "content/hooks.md"
      provides: "Hooks page with populated how-to and reference tabs"
      contains: "PreToolUse"
    - path: "content/agents.md"
      provides: "Agents page with populated how-to and reference tabs"
      contains: "permissionMode"
  key_links:
    - from: "content/hooks.md"
      to: "docs/topics/hooks.html"
      via: "build.js markdown-to-HTML pipeline"
      pattern: "tab-panel.*reference"
---

<objective>
Write how-to guides and technical reference specs for Skills, Plugins, Hooks, and Agents -- the four extension point topics with the most technical depth.

Purpose: These are the core extensibility mechanisms developers use to customize Claude Code. Each needs a walkthrough guide and a formal schema reference.
Output: 4 content files with populated how-to and reference tabs
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-guides-and-reference/04-RESEARCH.md
@content/skills.md
@content/plugins.md
@content/hooks.md
@content/agents.md

<interfaces>
<!-- Tab panel pattern: <div class="tab-panel" data-tab-panel="howto"> and data-tab-panel="reference"> -->
<!-- How-to template: Prerequisites, Step 1-N, Verify It Works, Troubleshooting -->
<!-- Reference template: File Location, Complete Schema, Field Reference table, Validation Rules, Examples -->
<!-- Test files created in Plan 01: tests/test-howto-content.js, tests/test-reference-content.js -->
<!-- Tests check for: "Prerequisites", "Step 1", "Field", "Type" in respective tab panels -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write how-to guides and reference specs for Skills and Plugins</name>
  <files>content/skills.md, content/plugins.md</files>
  <action>
Replace placeholder content in the howto and reference tab panels for both files.

**skills.md How-To tab (HOWT-05: How to create a skill with correct frontmatter):**
- Prerequisites: Claude Code installed, project with `.claude/` directory
- Step 1: Create skill directory: `mkdir -p .claude/skills`
- Step 2: Create skill file (e.g., `deploy-checker.md`) with complete frontmatter block showing all 10 fields: name, description, argument-hint, disable-model-invocation, user-invocable, allowed-tools, model, context, agent, hooks
- Step 3: Write the skill body (instructions for Claude) with realistic content for the deploy-checker example
- Step 4: Test the skill: invoke with `/deploy-checker staging` in Claude Code
- Step 5: Create a personal skill in `~/.claude/skills/` for cross-project reuse
- Verify It Works: skill appears in `/` command list, executes correctly
- Troubleshooting: skill not appearing (name validation -- max 64 chars, lowercase/hyphens only), frontmatter parse errors

**skills.md Reference tab (REFR-02: Skill frontmatter spec):**
- File Location: `.claude/skills/*.md` (project) or `~/.claude/skills/*.md` (personal)
- Complete frontmatter YAML example with all 10 fields
- Field Reference table with all 10 fields: name (string, required, max 64 chars, lowercase+hyphens), description (string, required, max 1024 chars, no XML tags), argument-hint (string, optional), disable-model-invocation (boolean, default false), user-invocable (boolean, default true), allowed-tools (string list, optional), model (string, optional), context (enum: fork|inherit, optional), agent (string, optional), hooks (object, optional)
- String substitutions: $ARGUMENTS, $ARGUMENTS[N], $N, ${CLAUDE_SESSION_ID}, ${CLAUDE_SKILL_DIR}
- Validation rules
- Minimal example + full example

Migrate any detailed skill config from Concept tab to Reference tab. Leave Concept with a lighter overview pointing to Reference for the complete spec.

**plugins.md How-To tab (HOWT-06: How to create a plugin that bundles skills):**
- Prerequisites: Skills or agents to bundle, directory structure
- Step 1: Create plugin directory structure: `.claude-plugin/plugin.json`, `skills/`, `agents/`, `hooks/`
- Step 2: Write plugin.json manifest (show complete example for a "deployment-tools" plugin)
- Step 3: Add skills to the skills/ directory
- Step 4: Test with `claude --plugin-dir ./path-to-plugin`
- Step 5: Distribution -- share via git, npm, or direct path
- Verify It Works: plugin loads, skills available
- Troubleshooting: manifest validation errors, skill loading issues

**plugins.md Reference tab (REFR-03: Plugin manifest spec):**
- File Location: `.claude-plugin/plugin.json`
- Complete JSON schema with all fields
- Field Reference table: name (string, required), version (string, optional), description (string, optional), author (object with name/email/url, optional), homepage (string, optional), repository (string, optional), license (string, optional), keywords (string[], optional), commands (string[], optional -- deprecated), agents (string, optional), skills (string, optional), hooks (string, optional), mcpServers (string, optional), outputStyles (string, optional), lspServers (string, optional)
- Auto-discovery behavior for conventionally-named directories
- Validation rules
- Minimal example + full example

Build and verify: `node build/build.js && node --test tests/test-howto-content.js 2>&1 | grep -E "skills|plugins"`
  </action>
  <verify>
    <automated>node build/build.js && node --test tests/test-howto-content.js 2>&1 | grep -E "skills|plugins|pass|fail" | head -20</automated>
  </verify>
  <done>Skills page has complete skill creation guide and frontmatter spec with all 10 fields. Plugins page has complete plugin creation guide and manifest spec. No placeholder text remains in skills/plugins how-to or reference panels. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Write how-to guides and reference specs for Hooks and Agents</name>
  <files>content/hooks.md, content/agents.md</files>
  <action>
Replace placeholder content in the howto and reference tab panels for both files.

**hooks.md How-To tab (HOWT-07: How to set up hooks for lifecycle events):**
- Prerequisites: Claude Code installed, settings.json accessible
- Step 1: Open settings -- explain where settings.json lives (project `.claude/settings.json` or user `~/.claude/settings.json`)
- Step 2: Add a PreToolUse hook for file linting (realistic example: run ESLint on files before Edit/Write tools)
  - Show the NESTED config structure per official docs: `{ "hooks": { "PreToolUse": [{ "matcher": "Edit|Write", "hooks": [{ "type": "command", "command": "npx eslint --fix $CLAUDE_FILE_PATH", "timeout": 600 }] }] } }`
- Step 3: Add a Notification hook (show NotificationSend with a command handler that sends to Slack/webhook)
- Step 4: Test the hook by triggering the event in Claude Code
- Verify It Works: hook fires on matching events
- Troubleshooting: hook not firing (check matcher regex), exit code 2 blocking behavior, timeout issues
- Note: Update any flat-structure examples in the Concept tab to use the correct nested structure

**hooks.md Reference tab (REFR-04: Hook configuration spec):**
- Configuration location: `settings.json` hooks object
- Complete nested schema example
- All 24 events in a table: event name, description, blockable (yes/no), matcher target
  - Events include: PreToolUse, PostToolUse, Notification, Stop, SubagentStop, PreCompact, SessionPause, SessionResume, TeammateIdle, TaskCompleted, InstructionsLoaded, Elicitation, etc.
- 4 handler types table: command (runs shell command), http (sends HTTP request), prompt (injects prompt text), agent (runs sub-agent)
  - For each type: all fields, types, required/optional
- Matcher syntax: regex pattern matching tool names
- Exit code behavior: 0 = allow, 1 = error, 2 = block
- JSON output schemas for each blockable event
- Environment variables available in hooks: $CLAUDE_FILE_PATH, etc.
- Minimal example + full example with multiple events

CRITICAL: Use the NESTED hook config structure from official docs, NOT the flat structure. Per research: "The Reference tab MUST use the correct nested schema from official docs."

Also update any flat-structure hook examples in the Concept tab to match the nested format for consistency.

**agents.md How-To tab (HOWT-08: How to create a custom agent):**
- Prerequisites: Claude Code installed, project with `.claude/` directory
- Step 1: Create agents directory: `mkdir -p .claude/agents`
- Step 2: Create agent file (e.g., `code-reviewer.md`) with frontmatter showing key fields: name, description, tools, disallowedTools, model, permissionMode, maxTurns
- Step 3: Write agent instructions (the markdown body) with realistic code-reviewer example
- Step 4: Test: `claude --agent code-reviewer`
- Step 5: Add advanced features -- skills, mcpServers, hooks, memory
- Step 6: Personal agents in `~/.claude/agents/` for cross-project use
- Verify It Works: agent loads with correct tool restrictions
- Troubleshooting: agent not found (file naming), permission mode issues

**agents.md Reference tab (REFR-05: Agent file format spec):**
- File Location: `.claude/agents/*.md` (project), `~/.claude/agents/*.md` (personal)
- Complete frontmatter YAML with all 12 fields
- Field Reference table: name (string), description (string), tools (string list), disallowedTools (string list), model (string), permissionMode (enum: default|acceptEdits|dontAsk|bypassPermissions|plan), maxTurns (number), skills (string list), mcpServers (array of objects or strings), hooks (object), memory (enum: project|user|none), background (boolean), isolation (enum: none|worktree)
- File resolution priority order
- Memory scopes explanation
- Minimal example + full example

Build and verify: `node build/build.js && npm test`
  </action>
  <verify>
    <automated>node build/build.js && npm test</automated>
  </verify>
  <done>Hooks page has complete hook setup guide and config spec with all 24 events and nested schema. Agents page has complete agent creation guide and file format spec with all 12 fields. Hook Concept tab examples updated to nested format. No placeholder text remains. All tests pass.</done>
</task>

</tasks>

<verification>
- `node build/build.js` succeeds
- `npm test` passes (all existing tests)
- `node --test tests/test-howto-content.js` -- skills, plugins, hooks, agents tests pass
- `node --test tests/test-reference-content.js` -- skills, plugins, hooks, agents tests pass
- No "coming in Phase 4" or "coming soon" text in any modified page's howto/reference panels
- Hook config examples use nested structure throughout (concept + reference tabs)
</verification>

<success_criteria>
- 4 how-to guides are complete walkthroughs (prerequisites through verification)
- 4 reference specs have complete field tables with types, required/optional, defaults
- All code examples use realistic values
- Hook config uses correct nested schema from official docs
- Build succeeds and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-guides-and-reference/04-02-SUMMARY.md`
</output>
