---
phase: 04-guides-and-reference
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - content/projects.md
  - content/memory.md
  - content/settings.md
  - content/connectors.md
  - content/commands.md
autonomous: true
requirements: [HOWT-09, HOWT-10, REFR-06, REFR-07, REFR-08]

must_haves:
  truths:
    - "Projects how-to tab has a complete CLAUDE.md configuration guide"
    - "Memory how-to tab has a complete memory system usage guide"
    - "Settings reference tab has the complete hierarchy resolution spec with all 5 levels"
    - "Projects reference tab has the complete CLAUDE.md file format and loading order"
    - "Settings reference tab has the environment variables reference table"
    - "Connectors and Commands pages have updated placeholder text (not 'coming in Phase 4')"
  artifacts:
    - path: "content/projects.md"
      provides: "Projects page with CLAUDE.md how-to guide and file format reference"
      contains: "CLAUDE.md"
    - path: "content/memory.md"
      provides: "Memory page with memory system how-to guide"
      contains: "/memory"
    - path: "content/settings.md"
      provides: "Settings page with hierarchy reference and env vars table"
      contains: "ANTHROPIC_MODEL"
  key_links:
    - from: "content/settings.md"
      to: "docs/topics/settings.html"
      via: "build.js markdown-to-HTML pipeline"
      pattern: "tab-panel.*reference"
---

<objective>
Write how-to guides for CLAUDE.md configuration and memory system, plus technical reference specs for settings hierarchy, CLAUDE.md format, and environment variables. Update Connectors and Commands placeholder text.

Purpose: Projects, Memory, and Settings are the configuration layer that every Claude Code user interacts with. The env vars reference (REFR-08) is a cross-cutting resource referenced by other pages.
Output: 5 content files updated -- 3 with full how-to/reference content, 2 with improved placeholders
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-guides-and-reference/04-RESEARCH.md
@content/projects.md
@content/memory.md
@content/settings.md
@content/connectors.md
@content/commands.md

<interfaces>
<!-- Tab panel pattern: <div class="tab-panel" data-tab-panel="howto"> and data-tab-panel="reference"> -->
<!-- How-to template: Prerequisites, Step 1-N, Verify It Works, Troubleshooting -->
<!-- Reference template: File Location, Complete Schema, Field Reference table, Validation Rules, Examples -->
<!-- Test files from Plan 01: tests/test-howto-content.js, tests/test-reference-content.js -->
<!-- REFR-08 (env vars) goes primarily in settings.md; other pages link to it -->
<!-- Connectors/Commands have no HOWT/REFR requirements -- just update placeholders -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write how-to guides and reference specs for Projects, Memory, and Settings</name>
  <files>content/projects.md, content/memory.md, content/settings.md</files>
  <action>
Replace placeholder content in howto and reference tab panels for all three files.

**projects.md How-To tab (HOWT-09: How to configure CLAUDE.md for a project):**
- Prerequisites: A project directory, Claude Code installed
- Step 1: Create initial CLAUDE.md at project root with project description, key conventions, and tech stack
- Step 2: Add directory-specific rules with `.claude/rules/*.md` files (e.g., `tests.md` with testing conventions, `api.md` with API patterns)
- Step 3: Create CLAUDE.local.md for personal preferences not shared with team (add to .gitignore)
- Step 4: Monorepo patterns -- CLAUDE.md at root + per-package CLAUDE.md files
- Step 5: Use @import syntax to reference shared instruction files
- Verify It Works: start a Claude Code session, ask "what are the project conventions?" -- Claude should reflect your CLAUDE.md content
- Troubleshooting: CLAUDE.md not loading (check file name case), rules not applying (check directory placement), file too large (character limits)

**projects.md Reference tab (REFR-07: CLAUDE.md file format and loading order):**
- File names recognized: CLAUDE.md, CLAUDE.local.md, .claude/rules/*.md
- Loading order with precedence: UP (parent directories), DOWN (child directories), SIBLING (.claude/rules/)
- @import syntax: `@path/to/file.md` for including shared instructions
- Character limits per scope
- Merge semantics: how multiple CLAUDE.md files combine (later overrides earlier for conflicting instructions)
- File location resolution table: project root, parent dirs, .claude/rules/, user home
- Minimal example + monorepo example

Migrate any detailed CLAUDE.md format info from Concept tab to Reference. Leave Concept with overview pointing to Reference.

**memory.md How-To tab (HOWT-10: How to use the memory system effectively):**
- Prerequisites: Claude Code installed
- Step 1: Understand the 4 layers -- CLAUDE.md (project), auto-memory (persisted learnings), /memory command (explicit saves), agent memory (per-agent)
- Step 2: Use `/memory` command to save important context: show example saving a coding convention
- Step 3: Configure auto-memory behavior (how Claude automatically remembers corrections)
- Step 4: Set up agent-scoped memory for specialized agents (memory: project vs user vs none)
- Step 5: Debug memory issues -- where to find memory files, how to clear stale memories
- Verify It Works: save something with /memory, start new session, verify it persists
- Troubleshooting: memory not persisting (check file permissions), too many memories (pruning)

**memory.md Reference tab:**
- Memory is documented primarily through its Concept overview (4-layer architecture)
- Reference tab should contain: memory file locations for each layer, file format for each memory type, agent memory scope options table, /memory command syntax reference
- Note: Memory doesn't have a standalone REFR requirement, but the reference tab should still be useful. Keep it focused on file locations and formats.

**settings.md Reference tab (REFR-06: Settings hierarchy resolution order + REFR-08: Environment variables):**

REFR-06 -- Settings hierarchy:
- 5-level precedence table: Managed > CLI flags > settings.local.json > settings.json (project) > settings.json (user)
- File locations per OS (macOS, Linux): user settings path, project settings path, managed settings path
- Complete settings.json schema example
- Permission modes: default, acceptEdits, dontAsk, bypassPermissions, plan -- what each allows
- Allow/deny syntax for tools and MCP servers
- Minimal example + full example

REFR-08 -- Environment variables reference:
- Table of ~20 most important env vars: | Variable | Type | Default | Description |
  Key vars to include: ANTHROPIC_MODEL, CLAUDE_CODE_EFFORT_LEVEL, CLAUDE_CODE_SUBAGENT_MODEL, CLAUDE_AUTOCOMPACT_PCT_OVERRIDE, MAX_THINKING_TOKENS, CLAUDE_CODE_MAX_OUTPUT_TOKENS, ENABLE_TOOL_SEARCH, ANTHROPIC_DEFAULT_HAIKU_MODEL (note deprecated ANTHROPIC_SMALL_FAST_MODEL), ANTHROPIC_BASE_URL, CLAUDE_CODE_DISABLE_BACKGROUND_TASKS, CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS, CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS, SLASH_COMMAND_TOOL_CHAR_BUDGET
- Note: "Full list of 140+ env vars available via `claude config list` or official docs"
- This is the canonical env vars reference -- other topic pages should link here

Also update settings.md How-To tab if it has placeholder content -- add practical settings configuration walkthrough if needed.

Migrate detailed config from Concept tabs to Reference tabs where appropriate for all three files.

Build and verify: `node build/build.js && npm test`
  </action>
  <verify>
    <automated>node build/build.js && npm test</automated>
  </verify>
  <done>Projects page has CLAUDE.md configuration guide and file format reference with loading order. Memory page has memory system usage guide and file location reference. Settings page has hierarchy spec with all 5 levels and env vars table with ~20 key variables. No placeholder "coming in Phase 4" text in any modified how-to or reference panel. Build succeeds and all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update Connectors and Commands placeholder text</name>
  <files>content/connectors.md, content/commands.md</files>
  <action>
Connectors and Commands have no HOWT/REFR requirements assigned for Phase 4, but their how-to and reference tabs still show "coming in Phase 4" placeholder text. Update these to be more helpful without writing full guides.

**connectors.md:**
- How-To tab: Replace "coming in Phase 4" with a brief note explaining that Connectors are managed through the Claude Chat interface (not Claude Code CLI). Point users to the MCP page for setting up tool servers in Claude Code: "For adding tool servers to Claude Code, see the [MCP How-To guides](mcp.html)."
- Reference tab: Replace placeholder with a brief note that Connectors are configured through the Claude Chat UI and don't have a CLI config schema. List the available Interactive Apps (Figma, Slack, Asana, etc.) if not already in the Concept tab.

**commands.md:**
- How-To tab: Replace "coming in Phase 4" with a note explaining that Commands are a legacy feature -- point users to the Skills page: "For creating custom slash commands, see the [Skills How-To guide](skills.html), which covers the recommended approach using Skills with frontmatter."
- Reference tab: Replace placeholder with a brief note about the legacy `.claude/commands/` directory format, and a clear pointer to the Skills Reference for the current recommended spec.

These updates should be brief (10-20 lines each tab) -- not full guides, just helpful redirects.

Build and verify: `node build/build.js && node --test tests/test-build.js`
  </action>
  <verify>
    <automated>node build/build.js && node --test tests/test-build.js</automated>
  </verify>
  <done>Connectors and Commands pages no longer show "coming in Phase 4" or "coming soon" text. Both pages redirect users to the appropriate topic (MCP for connectors, Skills for commands). Build succeeds.</done>
</task>

</tasks>

<verification>
- `node build/build.js` succeeds
- `npm test` passes (all tests including new howto/reference content tests)
- `node --test tests/test-howto-content.js` -- projects and memory tests pass
- `node --test tests/test-reference-content.js` -- settings and projects tests pass
- No "coming in Phase 4" or "coming soon" text remains on ANY topic page
- Settings reference tab has both hierarchy spec and env vars table
</verification>

<success_criteria>
- Projects page has complete CLAUDE.md guide (create, rules, local, monorepo) and file format reference
- Memory page has complete memory system guide (all 4 layers, /memory command, debugging)
- Settings page has complete hierarchy reference (5 levels, file locations, permissions) and env vars table (~20 key vars)
- Connectors and Commands pages have helpful redirect text instead of placeholders
- All code examples use realistic values
- Build succeeds and all tests pass
- No placeholder text remains anywhere in the site's how-to or reference tabs
</success_criteria>

<output>
After completion, create `.planning/phases/04-guides-and-reference/04-03-SUMMARY.md`
</output>
