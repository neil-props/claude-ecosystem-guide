---
phase: 05-decision-guidance-and-recipes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/recipe-deployment-skill.md
  - content/recipe-crm-mcp.md
  - content/recipe-pre-commit-hook.md
  - content/recipe-custom-agent.md
  - content/recipe-monorepo-settings.md
  - tests/test-recipes.js
autonomous: true
requirements:
  - RECP-01
  - RECP-02
  - RECP-03
  - RECP-04
  - RECP-05

must_haves:
  truths:
    - "User can follow a complete deployment skill recipe from start to finish"
    - "User can follow a CRM enrichment with MCP recipe end-to-end"
    - "User can follow a pre-commit hook recipe with working example"
    - "User can follow a custom agent recipe with real configuration"
    - "User can follow a monorepo settings recipe with hierarchy examples"
    - "All 5 recipe pages appear in sidebar under Recipes section"
  artifacts:
    - path: "content/recipe-deployment-skill.md"
      provides: "Deployment skill recipe"
      contains: "Step 1"
    - path: "content/recipe-crm-mcp.md"
      provides: "CRM MCP recipe"
      contains: "Step 1"
    - path: "content/recipe-pre-commit-hook.md"
      provides: "Pre-commit hook recipe"
      contains: "Step 1"
    - path: "content/recipe-custom-agent.md"
      provides: "Custom agent recipe"
      contains: "Step 1"
    - path: "content/recipe-monorepo-settings.md"
      provides: "Monorepo settings recipe"
      contains: "Step 1"
  key_links:
    - from: "content/recipe-*.md"
      to: "content/skills.md"
      via: "cross-reference links"
      pattern: "topics/(skills|mcp|hooks|agents|settings)\\.html"
---

<objective>
Create five end-to-end recipe pages covering common Claude Code workflows.

Purpose: Give users complete, copy-pasteable walkthroughs for real-world use cases that demonstrate extension points working together.
Output: Five recipe markdown files that build into HTML pages grouped under "Recipes" in the sidebar.
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-decision-guidance-and-recipes/05-RESEARCH.md

@content/skills.md
@content/mcp.md
@content/hooks.md
@content/agents.md
@content/settings.md
</context>

<interfaces>
<!-- Recipe pages use the same frontmatter pattern as existing content -->

From content/*.md frontmatter:
```yaml
---
title: "Page Title"
slug: page-slug
order: N
description: Description text
section: guides    # NEW section value for recipes
---
```

From build/build.js generateNav():
- Plan 01 extends this to handle `section: 'guides'` rendering under "Recipes" nav header
- Recipe pages will automatically appear once Plan 01's build changes are merged
- If Plan 02 executes first, pages still build correctly — they just won't have a nav section until Plan 01 completes

Recipe template structure (from 05-RESEARCH.md):
```markdown
# Recipe Title
## What You Will Build
## Prerequisites
## Step 1: ...
## Step 2: ...
## Verify It Works
## Next Steps
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Test scaffold and first 3 recipe pages</name>
  <files>
    tests/test-recipes.js
    content/recipe-deployment-skill.md
    content/recipe-crm-mcp.md
    content/recipe-pre-commit-hook.md
  </files>
  <action>
1. **Create tests/test-recipes.js**: Use node:test pattern (same as test-build.js). Run build in before hook. Tests:
   - All 5 recipe pages exist in `docs/topics/`: `recipe-deployment-skill.html`, `recipe-crm-mcp.html`, `recipe-pre-commit-hook.html`, `recipe-custom-agent.html`, `recipe-monorepo-settings.html`
   - Each recipe page contains "What You" (from "What You Will Build" or "What You Will Configure"), "Prerequisites", and "Verify" text
   - Each recipe page contains at least 2 code blocks (`.code-block` divs)
   - Each recipe page links to at least one topic page (href containing `topics/`)

2. **Create content/recipe-deployment-skill.md** (RECP-01):
   ```yaml
   title: "Recipe: Build a Deployment Skill"
   slug: recipe-deployment-skill
   order: 1
   description: "End-to-end walkthrough: create a skill that deploys your app"
   section: guides
   ```
   Structure — What You Will Build (a skill that guides Claude through deploying a Node.js app to a VPS via SSH), Prerequisites (Claude Code installed, basic familiarity with skills — link to skills topic), then steps:
   - Step 1: Create the skill directory — `.claude/skills/` in project root, create `deploy.md`
   - Step 2: Write the frontmatter — show complete frontmatter with `name`, `description`, `triggers` (glob patterns), `allowed-tools` (Bash), example:
     ```yaml
     ---
     name: deploy
     description: Deploy the application to production
     triggers:
       - deploy
       - push to production
     allowed-tools:
       - Bash
     ---
     ```
   - Step 3: Write the skill body — markdown instructions for Claude: check git status is clean, run tests, SSH to server, pull latest, restart service. Show realistic commands.
   - Step 4: Test the skill — invoke with "deploy the app" in Claude Code, verify Claude follows the steps
   - Verify It Works: describe expected behavior (Claude asks for confirmation, runs each step)
   - Next Steps: link to Skills reference for all frontmatter fields, link to Agents topic for more complex workflows

3. **Create content/recipe-crm-mcp.md** (RECP-02):
   ```yaml
   title: "Recipe: CRM Enrichment with MCP"
   slug: recipe-crm-mcp
   order: 2
   description: "End-to-end walkthrough: connect Claude to a CRM via MCP"
   section: guides
   ```
   Structure — What You Will Build (an MCP server that lets Claude look up and enrich customer records from a CRM API), Prerequisites (Node.js 18+, Claude Code, a CRM API key — use a generic REST API example), then steps:
   - Step 1: Scaffold the MCP server — create project directory, `npm init`, install `@modelcontextprotocol/sdk`
   - Step 2: Implement the server — show a complete `server.js` with stdio transport that exposes `lookup_customer` and `enrich_record` tools. Use realistic but generic code (fetch from REST API, return structured data).
   - Step 3: Configure Claude Code — show `.mcp.json` entry with `command: "node"`, `args: ["path/to/server.js"]`, and `env` for API key
   - Step 4: Test the integration — ask Claude "look up customer John Doe" and verify it uses the MCP tool
   - Verify It Works: Claude lists available MCP tools, successfully calls lookup
   - Next Steps: link to MCP reference for config schema, link to MCP how-to for SSE transport if hosting remotely

4. **Create content/recipe-pre-commit-hook.md** (RECP-03):
   ```yaml
   title: "Recipe: Pre-Commit Linting Hook"
   slug: recipe-pre-commit-hook
   order: 3
   description: "End-to-end walkthrough: enforce code quality with a hook"
   section: guides
   ```
   Structure — What You Will Build (a hook that runs ESLint before Claude commits code, blocking the commit if linting fails), Prerequisites (Claude Code, ESLint configured in project), then steps:
   - Step 1: Understand hook lifecycle — brief explanation of `PreCommit` event, link to Hooks concept page
   - Step 2: Configure the hook — show complete `.claude/settings.json` hook config:
     ```json
     {
       "hooks": {
         "PreCommit": [
           {
             "matcher": "**/*.{js,ts}",
             "command": "npx eslint --no-warn-ignored ${files}",
             "description": "Lint staged files before commit"
           }
         ]
       }
     }
     ```
   - Step 3: Test the hook — create a file with a linting error, ask Claude to commit, verify the hook blocks it
   - Step 4: Handle failures — explain that Claude sees the hook output and can fix issues automatically
   - Verify It Works: demonstrate the block-then-fix flow
   - Next Steps: link to Hooks reference for all event types, link to Settings for hook config placement
  </action>
  <verify>
    <automated>node --test tests/test-build.js</automated>
  </verify>
  <done>3 recipe pages build correctly, test scaffold created (will show failures for the 2 missing recipe pages, which is expected)</done>
</task>

<task type="auto">
  <name>Task 2: Remaining 2 recipe pages</name>
  <files>
    content/recipe-custom-agent.md
    content/recipe-monorepo-settings.md
  </files>
  <action>
1. **Create content/recipe-custom-agent.md** (RECP-04):
   ```yaml
   title: "Recipe: Build a Custom Agent"
   slug: recipe-custom-agent
   order: 4
   description: "End-to-end walkthrough: create a specialized agent for your team"
   section: guides
   ```
   Structure — What You Will Build (a code review agent that team members invoke to get thorough PR reviews), Prerequisites (Claude Code, a project with PRs to review), then steps:
   - Step 1: Create the agent file — `.claude/agents/code-reviewer.md` in project root
   - Step 2: Write the agent prompt — show complete agent file with system prompt that instructs Claude to: check out the PR branch, review changed files, look for bugs/security issues/style violations, write a structured review summary. Include `allowed-tools` for Bash and Read.
   - Step 3: Configure the agent — explain the agent file format, how `name`, `description`, and the markdown body work together
   - Step 4: Invoke the agent — `claude --agent code-reviewer "Review PR #42"`
   - Step 5: Customize for your team — show how to add team-specific rules (coding standards, forbidden patterns, required test coverage)
   - Verify It Works: agent produces structured review output
   - Next Steps: link to Agents reference, link to Skills topic for simpler instruction-based alternative

2. **Create content/recipe-monorepo-settings.md** (RECP-05):
   ```yaml
   title: "Recipe: Monorepo Settings Hierarchy"
   slug: recipe-monorepo-settings
   order: 5
   description: "End-to-end walkthrough: configure Claude Code settings across a monorepo"
   section: guides
   ```
   Structure — What You Will Configure (settings hierarchy for a monorepo with shared root config and per-package overrides), Prerequisites (Claude Code, a monorepo with multiple packages), then steps:
   - Step 1: Understand the settings hierarchy — brief recap of 5-level precedence, link to Settings concept page
   - Step 2: Create root settings — show `.claude/settings.json` at monorepo root with shared MCP servers, common hooks, team-wide model preferences
   - Step 3: Create package-level overrides — show `packages/frontend/.claude/settings.json` that adds React-specific MCP servers, overrides allowed tools
   - Step 4: Create per-developer local settings — show `.claude/settings.local.json` (gitignored) for personal API keys, model preferences
   - Step 5: Add CLAUDE.md at each level — root CLAUDE.md with monorepo conventions, package CLAUDE.md with package-specific instructions
   - Step 6: Verify the hierarchy — show how to check effective settings, demonstrate that package settings override root
   - Verify It Works: Claude in a package directory uses the merged config from all levels
   - Next Steps: link to Settings reference for all fields, link to Projects for CLAUDE.md details
  </action>
  <verify>
    <automated>node --test tests/test-recipes.js tests/test-build.js</automated>
  </verify>
  <done>All 5 recipe pages build, each has complete walkthrough structure with code examples, all appear in sidebar under Recipes, all tests pass</done>
</task>

</tasks>

<verification>
- `node --test tests/test-recipes.js` passes (all 5 recipe pages exist with correct structure)
- `node --test tests/test-build.js` passes (build works with 7 new content pages total across Plans 01+02)
- Each recipe has at least 4 steps, code examples, prerequisites, and verification section
- Recipes link to relevant topic pages (not duplicating reference content)
</verification>

<success_criteria>
- All 5 recipe pages build into docs/topics/ as HTML
- Each recipe is a complete end-to-end walkthrough with realistic code examples
- Recipes appear in sidebar under "Recipes" section (once Plan 01 build changes are in)
- No recipe duplicates content from reference tabs — they link instead
</success_criteria>

<output>
After completion, create `.planning/phases/05-decision-guidance-and-recipes/05-02-SUMMARY.md`
</output>
