---
phase: 1.1-design-system
plan: 02
type: execute
wave: 2
depends_on: ["1.1-01"]
files_modified:
  - docs/assets/style.css
  - docs/assets/main.js
  - content/index.md
  - build/build.js
  - build/templates/page.html
autonomous: false
requirements: [DSGN-03, DSGN-04, DSGN-06, DSGN-07]

must_haves:
  truths:
    - "Reusable CSS component classes exist for cards, callouts, badges, buttons, and eyebrow labels"
    - "Tab UI component allows switching between concept/howto/reference/decision-tree content views"
    - "Cards have rounded corners, shadow, and hover elevation"
    - "Badges are pill-shaped with brand color variants"
    - "Buttons are pill-shaped with primary/secondary/outline/ghost/icterine variants"
    - "Design showcase page demonstrates all components in both themes"
    - "Interactive elements (copy feedback, tab switching) work correctly"
  artifacts:
    - path: "docs/assets/style.css"
      provides: "Component library CSS classes"
      contains: ".card"
    - path: "docs/assets/main.js"
      provides: "Tab switching and interactive element JS"
      contains: "tab"
    - path: "content/index.md"
      provides: "Landing page updated with Props design showcase elements"
  key_links:
    - from: "docs/assets/style.css"
      to: "build/templates/page.html"
      via: "Component classes available globally via stylesheet"
      pattern: "\\.card|\\.badge|\\.btn|\\.callout|\\.tab"
    - from: "docs/assets/main.js"
      to: "docs/assets/style.css"
      via: "JS toggles active classes that CSS styles"
      pattern: "tab.*active|classList"
---

<objective>
Build the reusable component library (cards, callouts, badges, buttons, tabs, eyebrow labels) and interactive element styles, then create a design showcase on the landing page for visual approval.

Purpose: Complete the Props design system implementation with all UI components needed for content phases, and get visual sign-off before proceeding.
Output: Full component CSS library, tab switching JS, and a showcase page demonstrating all components.
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1.1-design-system/1.1-CONTEXT.md
@.planning/phases/1.1-design-system/1.1-01-SUMMARY.md

@docs/assets/style.css
@docs/assets/main.js
@content/index.md
@build/build.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Component library CSS and tab switching JS</name>
  <files>docs/assets/style.css, docs/assets/main.js, build/build.js, build/templates/page.html</files>
  <action>
**Append component classes to docs/assets/style.css** (after the existing core styles from Plan 01):

**Buttons** (pill-shaped, 5 variants):
```css
.btn {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-2) var(--space-4);
  border-radius: var(--radius-full);  /* 100px - pill shape */
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  font-weight: 600;
  cursor: pointer;
  transition: all var(--transition-fast);
  border: 2px solid transparent;
  text-decoration: none;
}
.btn-primary { background: var(--turquoise); color: var(--oxford-blue); border-color: var(--turquoise); }
.btn-primary:hover { background: var(--turquoise-700); border-color: var(--turquoise-700); }
.btn-secondary { background: var(--oxford-blue); color: var(--text-only); border-color: var(--oxford-blue); }
.btn-secondary:hover { background: var(--oxford-700); }
.btn-outline { background: transparent; color: var(--accent); border-color: var(--accent); }
.btn-outline:hover { background: var(--accent); color: var(--oxford-blue); }
.btn-ghost { background: transparent; color: var(--accent); border-color: transparent; }
.btn-ghost:hover { background: rgba(81, 221, 205, 0.1); }
.btn-icterine { background: var(--icterine); color: var(--oxford-blue); border-color: var(--icterine); }
.btn-icterine:hover { background: var(--icterine-700); border-color: var(--icterine-700); }
```

**Badges** (pill-shaped with optional dot):
```css
.badge {
  display: inline-flex;
  align-items: center;
  gap: 0.375rem;
  padding: 0.25rem 0.75rem;
  border-radius: var(--radius-full);
  font-family: var(--font-sans);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.02em;
}
.badge-dot::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: currentColor;
}
.badge-turquoise { background: rgba(81, 221, 205, 0.15); color: var(--turquoise); }
.badge-icterine { background: rgba(255, 250, 120, 0.15); color: var(--icterine-700); }
.badge-oxford { background: rgba(0, 22, 57, 0.15); color: var(--oxford-500); }
.badge-success { background: rgba(81, 221, 205, 0.15); color: var(--turquoise); }
.badge-warning { background: rgba(255, 250, 120, 0.15); color: var(--icterine-700); }
.badge-info { background: rgba(0, 79, 171, 0.15); color: var(--oxford-500); }
```
Light theme overrides for badge colors where needed.

**Cards:**
```css
.card {
  background: var(--surface);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
  padding: var(--space-5);
  transition: box-shadow var(--transition-fast), transform var(--transition-fast);
  border: 1px solid var(--border);
}
.card:hover {
  box-shadow: var(--shadow-lg);
  transform: translateY(-2px);
}
.card-title {
  font-family: var(--font-serif);
  font-size: var(--text-h4);
  font-weight: 600;
  margin-bottom: var(--space-2);
  color: var(--text);
}
.card-body { color: var(--text-dim); }
```

**Callouts** (info, warning, tip, danger):
```css
.callout {
  padding: var(--space-4);
  border-radius: var(--radius-md);
  border-left: 4px solid;
  margin: var(--space-4) 0;
}
.callout-title {
  font-family: var(--font-sans);
  font-weight: 600;
  font-size: var(--text-sm);
  margin-bottom: var(--space-2);
}
.callout-info { border-color: var(--turquoise); background: rgba(81, 221, 205, 0.08); }
.callout-info .callout-title { color: var(--turquoise); }
.callout-warning { border-color: var(--icterine); background: rgba(255, 250, 120, 0.08); }
.callout-warning .callout-title { color: var(--icterine-700); }
.callout-tip { border-color: var(--turquoise-700); background: rgba(55, 202, 189, 0.08); }
.callout-tip .callout-title { color: var(--turquoise-700); }
.callout-danger { border-color: #FF8A65; background: rgba(255, 138, 101, 0.08); }
.callout-danger .callout-title { color: #FF8A65; }
```

**Eyebrow labels:**
```css
.eyebrow {
  font-family: var(--font-sans);
  font-size: var(--text-xs);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.2em;
  display: inline-block;
}
.eyebrow-turquoise { color: var(--turquoise); }
.eyebrow-icterine { color: var(--icterine); }
.eyebrow-oxford { color: var(--oxford-500); }
```

**Tabs** (for content layer UI -- concept/howto/reference/decision-tree):
```css
.tab-group {
  display: flex;
  gap: 0;
  border-bottom: 2px solid var(--border);
  margin-bottom: var(--space-5);
  overflow-x: auto;
}
.tab-btn {
  padding: var(--space-3) var(--space-4);
  background: transparent;
  border: none;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  font-weight: 500;
  color: var(--text-muted);
  cursor: pointer;
  transition: color var(--transition-fast), border-color var(--transition-fast);
  white-space: nowrap;
}
.tab-btn:hover { color: var(--text); }
.tab-btn.active {
  color: var(--accent);
  border-bottom-color: var(--accent);
  font-weight: 600;
}
.tab-panel { display: none; }
.tab-panel.active { display: block; }
```

**In docs/assets/main.js -- add tab switching logic:**
After the existing DOMContentLoaded handlers, add:
```javascript
// Tab switching
document.addEventListener('click', function(e) {
  var tabBtn = e.target.closest('.tab-btn');
  if (!tabBtn) return;

  var group = tabBtn.closest('.tabs');
  if (!group) return;

  var tabId = tabBtn.getAttribute('data-tab');

  // Deactivate all tabs in this group
  group.querySelectorAll('.tab-btn').forEach(function(btn) {
    btn.classList.remove('active');
  });
  group.querySelectorAll('.tab-panel').forEach(function(panel) {
    panel.classList.remove('active');
  });

  // Activate selected tab
  tabBtn.classList.add('active');
  var panel = group.querySelector('[data-tab-panel="' + tabId + '"]');
  if (panel) panel.classList.add('active');
});
```

**In build/build.js -- add custom renderer for callouts:**
Add a blockquote renderer that converts `> [!INFO]`, `> [!WARNING]`, `> [!TIP]`, `> [!DANGER]` blockquotes into callout divs. Parse the first line for the type indicator, wrap content in `.callout.callout-{type}` div with `.callout-title` for the label.

**In build/templates/page.html:**
Add a `<div class="sidebar-overlay" id="sidebarOverlay"></div>` element (if not already present) for mobile sidebar overlay. Ensure it sits between the sidebar and content.
  </action>
  <verify>
    <automated>cd /Users/sharode/Desktop/claude-ecosystem-guide && node build/build.js 2>&1 && npm test 2>&1 && grep -c "\.card" docs/assets/style.css && grep -c "\.badge" docs/assets/style.css && grep -c "\.tab-btn" docs/assets/style.css && grep -c "\.callout" docs/assets/style.css && grep -c "\.btn-primary" docs/assets/style.css</automated>
  </verify>
  <done>Component library CSS classes exist for: buttons (5 variants), badges (6 variants + dot), cards (with hover elevation), callouts (4 types), eyebrow labels (3 colors), tabs (with active indicator). Tab switching JS works. Callout blockquote rendering in build script converts markdown callouts to styled divs. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Design showcase on landing page</name>
  <files>content/index.md</files>
  <action>
Update `content/index.md` to serve as both the landing page and a design system showcase. Keep the existing landing page content (extension point overview, links to topics) but enhance it with Props design system elements to demonstrate the components.

Add the following sections AFTER the existing introductory content:

**Hero section update:**
- Keep existing h1 but the markdown should render well with the serif heading styles
- Add an eyebrow label above it: use raw HTML `<span class="eyebrow eyebrow-turquoise">Reference Guide</span>`

**Component Showcase section** (h2: "Design System"):
This section demonstrates all components for visual review. Use raw HTML blocks in the markdown:

1. **Buttons row:**
```html
<div style="display: flex; flex-wrap: wrap; gap: 0.75rem; margin: 1.5rem 0;">
  <a class="btn btn-primary" href="#">Primary</a>
  <a class="btn btn-secondary" href="#">Secondary</a>
  <a class="btn btn-outline" href="#">Outline</a>
  <a class="btn btn-ghost" href="#">Ghost</a>
  <a class="btn btn-icterine" href="#">Icterine</a>
</div>
```

2. **Badges row:**
```html
<div style="display: flex; flex-wrap: wrap; gap: 0.5rem; margin: 1.5rem 0;">
  <span class="badge badge-turquoise">Turquoise</span>
  <span class="badge badge-icterine">Icterine</span>
  <span class="badge badge-oxford">Oxford</span>
  <span class="badge badge-dot badge-success">Active</span>
  <span class="badge badge-dot badge-warning">Beta</span>
  <span class="badge badge-dot badge-info">Info</span>
</div>
```

3. **Cards grid** (2 cards side by side):
```html
<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.5rem; margin: 1.5rem 0;">
  <div class="card">
    <div class="card-title">MCP Servers</div>
    <div class="card-body">Connect Claude to external tools and data sources using the Model Context Protocol.</div>
  </div>
  <div class="card">
    <div class="card-title">Custom Skills</div>
    <div class="card-body">Teach Claude new capabilities with markdown-based skill files and frontmatter.</div>
  </div>
</div>
```

4. **Callouts** (all 4 types):
Use markdown blockquotes with the callout syntax:
```markdown
> [!INFO]
> This is an info callout for general information.

> [!TIP]
> This is a tip callout for helpful suggestions.

> [!WARNING]
> This is a warning callout for important caveats.

> [!DANGER]
> This is a danger callout for critical warnings.
```

5. **Tabs demo:**
```html
<div class="tabs">
  <div class="tab-group">
    <button class="tab-btn active" data-tab="concept">Concept</button>
    <button class="tab-btn" data-tab="howto">How-To</button>
    <button class="tab-btn" data-tab="reference">Reference</button>
  </div>
  <div class="tab-panel active" data-tab-panel="concept">
    <p>Conceptual overview content would appear here, explaining what a feature is and when to use it.</p>
  </div>
  <div class="tab-panel" data-tab-panel="howto">
    <p>Step-by-step guide content would appear here, walking through setup and configuration.</p>
  </div>
  <div class="tab-panel" data-tab-panel="reference">
    <p>Technical reference content would appear here, documenting schemas, fields, and validation rules.</p>
  </div>
</div>
```

6. **Eyebrow labels:**
```html
<div style="display: flex; gap: 1.5rem; margin: 1.5rem 0;">
  <span class="eyebrow eyebrow-turquoise">Feature</span>
  <span class="eyebrow eyebrow-icterine">New</span>
  <span class="eyebrow eyebrow-oxford">Reference</span>
</div>
```

Keep ALL existing content (extension point links, etc.) -- just add the showcase section. The existing code block example should naturally demonstrate the new code block styling.
  </action>
  <verify>
    <automated>cd /Users/sharode/Desktop/claude-ecosystem-guide && node build/build.js 2>&1 && grep -c "btn-primary" docs/index.html && grep -c "card" docs/index.html && grep -c "tab-btn" docs/index.html && grep -c "callout" docs/index.html && grep -c "badge" docs/index.html</automated>
  </verify>
  <done>Landing page contains showcase sections for: buttons (5 variants), badges (6 variants), cards (2 example cards), callouts (4 types), tabs (3-panel demo), and eyebrow labels (3 colors). All components render using Props design system styles. Existing landing page content preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual design approval</name>
  <files></files>
  <action>Human reviews the complete Props design system implementation in the browser.</action>
  <what-built>Complete Props design system applied to the Claude Ecosystem Guide: design tokens, typography (IBM Plex Serif headings, Inter body), dark/light themes (Oxford Blue / Isabelline), sidebar with turquoise accents, component library (buttons, badges, cards, callouts, tabs, eyebrow labels), and code block restyling. All demonstrated on the landing page.</what-built>
  <how-to-verify>
    1. Run the build: `cd ~/Desktop/claude-ecosystem-guide && npm run build`
    2. Open `docs/index.html` in a browser
    3. Verify dark theme: Oxford Blue background, near-white text, turquoise accents
    4. Click the theme toggle: verify Isabelline background, Oxford Blue text
    5. Scroll to "Design System" section -- check all components:
       - 5 button variants (pill-shaped)
       - 6 badge variants (pill-shaped, some with dots)
       - 2 cards (with shadow, hover elevation)
       - 4 callout types (info/tip/warning/danger with colored left border)
       - Tab UI (click between Concept/How-To/Reference)
       - 3 eyebrow labels
    6. Verify sidebar: Oxford Blue background, turquoise active nav link indicator
    7. Verify code blocks: Oxford Blue background, turquoise keywords, icterine strings
    8. Verify headings use serif font (IBM Plex Serif), body uses sans-serif (Inter)
    9. Resize browser to < 870px -- verify sidebar hides, hamburger appears
    10. Click hamburger -- verify sidebar slides in, X animation
    11. Navigate to any topic page -- verify consistent styling
  </how-to-verify>
  <verify>Human visual inspection confirms design system renders correctly in both themes</verify>
  <done>Human has approved the Props design system implementation visually</done>
  <resume-signal>Type "approved" to proceed to Phase 2, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Build succeeds: `node build/build.js` completes
2. All tests pass: `npm test` green
3. Component classes present in CSS: .card, .badge, .btn, .callout, .tab-btn, .eyebrow
4. Tab switching works in JS: main.js contains tab click handler
5. Showcase page renders all components: index.html contains all component HTML
6. Human visual approval received
</verification>

<success_criteria>
- Complete CSS component library with buttons, badges, cards, callouts, tabs, and eyebrow labels
- Tab UI switches between content panels (concept/howto/reference)
- Callouts render from markdown blockquote syntax via build script
- Landing page showcases all components for visual approval
- Human verifies both themes look correct with Props design system
- Human approves mobile responsive behavior
</success_criteria>

<output>
After completion, create `.planning/phases/1.1-design-system/1.1-02-SUMMARY.md`
</output>
