---
phase: 1.1-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/style.css
  - build/templates/page.html
  - docs/assets/main.js
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-05]

must_haves:
  truths:
    - "Dark theme uses Oxford Blue (#001639) background with near-white (#FEFDFD) text"
    - "Light theme uses Isabelline (#EBE6E0) background with Oxford Blue text"
    - "IBM Plex Serif is used for headings (h1-h4) and display text"
    - "Inter is used for body text, UI elements, and navigation"
    - "Sidebar has Oxford Blue background with turquoise active indicator"
    - "Mobile hamburger animates to X on open, sidebar slides from left"
    - "Code blocks use Oxford Blue background with Props syntax colors"
    - "All spacing uses the fluid 8-step clamp() scale"
    - "Turquoise accent links replace purple placeholder accent"
  artifacts:
    - path: "docs/assets/style.css"
      provides: "Complete Props design token system and core styles"
      contains: "--oxford-blue"
    - path: "build/templates/page.html"
      provides: "Google Fonts imports for IBM Plex Serif and Inter"
      contains: "IBM+Plex+Serif"
  key_links:
    - from: "build/templates/page.html"
      to: "docs/assets/style.css"
      via: "Google Fonts link + CSS custom properties"
      pattern: "fonts.googleapis.com.*IBM.Plex.Serif"
    - from: "docs/assets/style.css"
      to: "build/templates/page.html"
      via: "Sidebar structure uses Oxford Blue bg, turquoise active states"
      pattern: "--oxford-blue|--turquoise"
---

<objective>
Replace all placeholder CSS with the Props design system: design tokens (colors, typography, spacing, shadows, transitions, radii), dark/light theme definitions, core layout styles (sidebar, content area, typography, code blocks, tables, lists), and mobile responsive patterns.

Purpose: Establish the complete visual foundation so all subsequent component and content work uses the Props design language consistently.
Output: Fully themed site with Props colors, typography, and layout patterns applied to all existing elements.
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1.1-design-system/1.1-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

@docs/assets/style.css
@build/templates/page.html
@docs/assets/main.js

<interfaces>
<!-- Current CSS custom properties to REPLACE (from style.css :root) -->
Current dark theme vars: --bg, --surface, --surface2, --surface3, --border, --border-hover, --text, --text-dim, --text-muted
Current accent vars: --code, --chat, --cowork, --mcp, --skill, --plugin, --project, --hook, --agent, --connector
Current light theme: [data-theme="light"] overrides same vars

<!-- Template structure (from page.html) -->
Template uses: {{title}}, {{content}}, {{nav}}, {{basePath}}
Has: .sidebar > .sidebar-header + .sidebar-nav, .mobile-nav-toggle, .content > article
Font import: Currently only Inter via Google Fonts

<!-- JS interactions (from main.js) -->
Theme toggle: reads/sets data-theme attribute, localStorage key 'claude-guide-theme'
Mobile nav: toggles .sidebar-open and .nav-active classes
Copy button: .copy-btn inside .code-block
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Props design tokens and Google Fonts setup</name>
  <files>docs/assets/style.css, build/templates/page.html</files>
  <action>
**In build/templates/page.html:**
Replace the existing Google Fonts import with one that loads both IBM Plex Serif (weights 400, 600, 700) and Inter (weights 400, 500, 600, 700, 800):
```
https://fonts.googleapis.com/css2?family=IBM+Plex+Serif:wght@400;600;700&family=Inter:wght@400;500;600;700;800&display=swap
```
Add this as a `<link>` tag in the `<head>` section (replacing or supplementing the existing font approach in style.css).

**In docs/assets/style.css — complete rewrite of :root and [data-theme="light"]:**

Replace the existing `:root` block with the full Props design token system:

**Color tokens:**
- `--oxford-blue: #001639` (primary dark)
- `--isabelline: #EBE6E0` (primary light)
- `--turquoise: #51DDCD` (accent 1)
- `--icterine: #FFFA78` (accent 2)
- `--text-only: #FEFDFD` (near-white)
- Oxford tints: `--oxford-900: #001639`, `--oxford-700: #002859`, `--oxford-500: #004FAB`
- Isabelline tints: `--isabelline-700: #C4BBB1`, `--isabelline-300: #F0ECE8`, `--isabelline-100: #F8F6F4`
- Turquoise tints: `--turquoise-700: #37CABD`, `--turquoise-300: #8AE8DE`, `--turquoise-100: #C5F4EF`
- Icterine tints: `--icterine-700: #E5E06C`, `--icterine-300: #FFFCA3`, `--icterine-100: #FFFED1`
- Neutrals: `--dark-gray: #1a1a1a`, `--medium-gray: #464646`, `--muted-gray: #7E899A`

**Typography tokens:**
- `--font-serif: 'IBM Plex Serif', Georgia, 'Times New Roman', serif`
- `--font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif`
- `--font-mono: 'SF Mono', 'Fira Code', 'Consolas', monospace`
- Fluid type scale using clamp():
  - `--text-display: clamp(4rem, 5vw + 2rem, 7rem)`
  - `--text-h1: clamp(2.25rem, 3vw + 1rem, 3.5rem)`
  - `--text-h2: clamp(1.5rem, 2vw + 0.5rem, 2.25rem)`
  - `--text-h3: clamp(1.25rem, 1.5vw + 0.5rem, 1.75rem)`
  - `--text-h4: clamp(1.1rem, 1vw + 0.5rem, 1.25rem)`
  - `--text-body: 1rem`
  - `--text-sm: 0.875rem`
  - `--text-xs: 0.75rem`

**Spacing tokens (fluid 8-step):**
- `--space-1: clamp(0.25rem, 0.5vw, 0.5rem)`
- `--space-2: clamp(0.5rem, 1vw, 0.75rem)`
- `--space-3: clamp(0.75rem, 1.5vw, 1rem)`
- `--space-4: clamp(1rem, 2vw, 1.5rem)`
- `--space-5: clamp(1.5rem, 3vw, 2rem)`
- `--space-6: clamp(2rem, 4vw, 3rem)`
- `--space-7: clamp(3rem, 5vw, 4rem)`
- `--space-8: clamp(4rem, 6vw, 6rem)`
- `--section-sm: var(--space-6)`
- `--section-md: var(--space-7)`
- `--section-lg: var(--space-8)`
- `--section-page-top: var(--space-7)`

**Border radius tokens:**
- `--radius-sm: 0.5rem`
- `--radius-md: 1rem`
- `--radius-lg: clamp(1.5rem, 2vw, 2rem)`
- `--radius-xl: clamp(2rem, 3vw, 3.125rem)`
- `--radius-full: 100px`

**Shadow tokens (using Oxford Blue rgba):**
- `--shadow-sm: 0 1px 2px rgba(0, 22, 57, 0.05)`
- `--shadow-md: 0 4px 6px -1px rgba(0, 22, 57, 0.1), 0 2px 4px -2px rgba(0, 22, 57, 0.1)`
- `--shadow-lg: 0 10px 15px -3px rgba(0, 22, 57, 0.1), 0 4px 6px -4px rgba(0, 22, 57, 0.1)`
- `--shadow-xl: 0 20px 25px -5px rgba(0, 22, 57, 0.1), 0 8px 10px -6px rgba(0, 22, 57, 0.1)`

**Transition tokens:**
- `--transition-fast: 150ms ease`
- `--transition-base: 300ms ease`
- `--transition-slow: 500ms ease`

**Gradient tokens:**
- `--gradient-oxford-deep: linear-gradient(135deg, #001639, #002859)`
- `--gradient-oxford-bright: linear-gradient(135deg, #001639, #004FAB)`
- `--gradient-turquoise-fade: linear-gradient(135deg, #37CABD, #51DDCD)`
- `--gradient-turquoise-icterine: linear-gradient(135deg, #51DDCD, #FFFA78)`
- `--gradient-oxford-turquoise: linear-gradient(135deg, #001639, #51DDCD)`
- `--gradient-isabelline-soft: linear-gradient(135deg, #EBE6E0, #FEFDFD)`

**Semantic mapping vars (theme-aware, set differently in dark vs light):**
- Dark (`:root`): `--bg: var(--oxford-blue)`, `--surface: #001E4D`, `--surface2: #002666`, `--surface3: #003080`, `--border: #003D99`, `--border-hover: #0050CC`, `--text: var(--text-only)`, `--text-dim: #B8C4D6`, `--text-muted: var(--muted-gray)`, `--accent: var(--turquoise)`, `--accent-secondary: var(--icterine)`
- Light (`[data-theme="light"]`): `--bg: var(--isabelline)`, `--surface: #FFFFFF`, `--surface2: var(--isabelline-300)`, `--surface3: var(--isabelline-700)`, `--border: #D0C9C0`, `--border-hover: #A89F94`, `--text: var(--oxford-blue)`, `--text-dim: var(--medium-gray)`, `--text-muted: var(--muted-gray)`, `--accent: var(--turquoise-700)`, `--accent-secondary: var(--icterine-700)`

Add `-webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale;` to body.

**Accessibility:** Add focus ring style:
```css
:focus-visible {
  outline: 2px solid var(--turquoise);
  outline-offset: 3px;
}
```
  </action>
  <verify>
    <automated>cd /Users/sharode/Desktop/claude-ecosystem-guide && node build/build.js 2>&1 && grep -c "oxford-blue" docs/assets/style.css && grep -c "IBM.Plex.Serif" build/templates/page.html</automated>
  </verify>
  <done>All Props design tokens defined as CSS custom properties in :root. Google Fonts link loads IBM Plex Serif and Inter. Dark theme uses Oxford Blue background, light theme uses Isabelline. Semantic vars (--bg, --text, --accent, etc.) map to design tokens in both themes.</done>
</task>

<task type="auto">
  <name>Task 2: Core layout and element styles using Props tokens</name>
  <files>docs/assets/style.css, docs/assets/main.js</files>
  <action>
Update ALL existing style rules in style.css to use the new Props design tokens. Do NOT remove any existing selectors -- update their values. Keep the same CSS structure/ordering.

**Body:**
- `font-family: var(--font-sans)` (already Inter, but use the token)
- Grid stays `260px 1fr`

**Sidebar:**
- Background: `var(--oxford-blue)` (always dark regardless of theme -- this is a brand element)
- Border-right: `1px solid rgba(255,255,255,0.1)` (subtle separator on dark bg)

**Sidebar header:**
- `.site-title`: `font-family: var(--font-serif)`, `font-size: 1.75rem`, `color: var(--turquoise)`, `letter-spacing: 0.15em`
- Add horizontal rules: `border-top` and `border-bottom` on `.sidebar-header` using `rgba(255,255,255,0.15)`

**Sidebar nav links:**
- Color: `var(--text-only)` with `opacity: 0.7`
- Hover: `opacity: 1`, `border-left: 3px solid var(--turquoise)`, subtle bg `rgba(255,255,255,0.05)`
- Active: `border-left-color: var(--turquoise)`, `background: rgba(81, 221, 205, 0.1)`, full opacity
- Font: `var(--font-sans)`, `var(--text-sm)`

**Nav section title:**
- `font-family: var(--font-sans)`, uppercase, `letter-spacing: 0.08em`, `color: var(--muted-gray)`

**Theme toggle (in sidebar):**
- Border and text use `rgba(255,255,255,0.3)`, hover brightens

**Typography (article content):**
- `h1`: `font-family: var(--font-serif)`, `font-size: var(--text-h1)`, `font-weight: 700`
- `h2`: `font-family: var(--font-serif)`, `font-size: var(--text-h2)`, `font-weight: 700`, bottom border uses `var(--border)`
- `h3`: `font-family: var(--font-serif)`, `font-size: var(--text-h3)`, `font-weight: 600`
- `h4`: `font-family: var(--font-serif)`, `font-size: var(--text-h4)`, `font-weight: 600`
- Body text: `var(--font-sans)`, `var(--text-body)`
- Links: `color: var(--accent)` with animated underline on hover (0 to 100% width using `background-image: linear-gradient(var(--accent), var(--accent))`, `background-size`, and transition)

**Heading anchors:**
- Hover color: `var(--accent)` (was --code)

**Code blocks:**
- `.code-block` background: `var(--oxford-blue)` (always dark, regardless of theme)
- `.code-header` background: `rgba(0, 22, 57, 0.8)` or slightly lighter oxford shade
- `.code-lang`: `var(--muted-gray)`
- `.copy-btn`: border `rgba(255,255,255,0.2)`, color `var(--muted-gray)`, hover color `var(--turquoise)`, hover border `var(--turquoise)`
- `pre code`: `font-family: var(--font-mono)`
- Inline code (`article p code, article li code`): background `var(--surface2)`, color `var(--accent)`, border `var(--border)`

**Syntax highlighting overrides:**
- `.hljs-keyword, .hljs-selector-tag, .hljs-built_in, .hljs-name`: `color: var(--turquoise)` (was --hook)
- `.hljs-string, .hljs-attr`: `color: var(--icterine)` (was --agent)
- `.hljs-number, .hljs-literal`: `color: #FF8A65` (warm orange for numbers per CONTEXT.md)
- `.hljs-comment`: `color: var(--muted-gray)` (was --text-muted)
- `.hljs-function .hljs-title, .hljs-title.function_`: `color: var(--turquoise-300)` (functions)
- Other hljs classes: adjust to use Props palette colors

**Tables:**
- `thead th`: background `var(--oxford-blue)`, color `var(--text-only)`
- `tbody tr:hover td`: background `var(--isabelline-300)` in light mode, `rgba(81,221,205,0.05)` in dark
- Borders: `var(--border)`

**Lists:**
- `article ul`: custom bullet using `::marker` or `li::before` with turquoise dot (8px circle)
- `article ol`: turquoise circular numbered badges (28px) -- use `li::before` with counter, `background: var(--turquoise)`, `color: var(--oxford-blue)`, `border-radius: 50%`, `width: 28px`, `height: 28px`, centered text

**Content area:**
- `.content` padding uses `var(--section-page-top)` for top, `var(--space-5)` for sides
- `max-width: 1120px` (was 800px -- per CONTEXT.md container max)

**Mobile responsive (update breakpoints):**
- Keep 640px mobile breakpoint but change tablet to `870px` per CONTEXT.md
- At `<= 870px`: hide sidebar completely (not just narrow it)
- Mobile sidebar: Oxford Blue bg, slides from left
- Hamburger: 3 lines, `1.5rem` wide, `2px` thick, `0.4rem` gap
- Open state: rotation animation 45/135deg, 300ms ease
- Sidebar overlay on mobile when open

**Copy feedback in main.js:**
Update the "Copied!" feedback to briefly add a class that shows turquoise color, then revert:
- When copy succeeds: set `btn.textContent = 'Copied!'` and add class `copy-success` to btn
- Add CSS: `.copy-success { color: var(--turquoise) !important; border-color: var(--turquoise) !important; }`
- Remove class after 2s along with text revert

**Theme transition:**
Keep the existing theme-transition class pattern but use `var(--transition-base)` timing.
  </action>
  <verify>
    <automated>cd /Users/sharode/Desktop/claude-ecosystem-guide && node build/build.js 2>&1 && npm test 2>&1</automated>
  </verify>
  <done>All existing CSS rules updated to use Props design tokens. Sidebar is Oxford Blue with turquoise accents. Typography uses IBM Plex Serif for headings, Inter for body. Code blocks use Oxford Blue bg with turquoise/icterine syntax colors. Tables have Oxford Blue headers. Lists have turquoise bullets/numbers. Mobile breakpoint at 870px with hamburger animation. All existing tests pass. Copy feedback shows turquoise flash.</done>
</task>

</tasks>

<verification>
1. Build succeeds: `node build/build.js` completes without errors
2. All existing tests pass: `npm test` passes all test suites
3. Design tokens present: `grep -c "oxford-blue" docs/assets/style.css` returns > 0
4. Google Fonts loaded: `grep "IBM.Plex.Serif" build/templates/page.html` finds the font link
5. Both themes defined: CSS contains both `:root` and `[data-theme="light"]` blocks with Props colors
6. Sidebar uses Oxford Blue: `grep "oxford-blue" docs/assets/style.css` matches sidebar rules
</verification>

<success_criteria>
- Props design token system fully defined in CSS custom properties
- Dark theme: Oxford Blue background, near-white text, turquoise/icterine accents
- Light theme: Isabelline background, Oxford Blue text, turquoise/icterine accents
- IBM Plex Serif loads for headings, Inter loads for body text
- Sidebar always uses Oxford Blue background with turquoise active indicators
- Code blocks always use Oxford Blue background regardless of theme
- Mobile navigation hides sidebar at 870px breakpoint
- All existing Phase 1 tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/1.1-design-system/1.1-01-SUMMARY.md`
</output>
