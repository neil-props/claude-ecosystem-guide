---
phase: 02-knowledge-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-knowledge-content.js
  - tests/test-knowledge-structure.js
  - tests/test-content-completeness.js
  - tests/test-ama-content.js
  - content/mcp.md
  - content/skills.md
  - content/plugins.md
  - content/hooks.md
  - content/agents.md
autonomous: true
requirements: [KNOW-01, KNOW-02, KNOW-03, KNOW-04]

must_haves:
  truths:
    - "MCP, Skills, Plugins, Hooks, and Agents content files contain real extracted content (not placeholder)"
    - "Each topic file follows consistent section template (Overview, How It Works, Configuration, Common Questions)"
    - "Key facts from index.html are preserved in the appropriate content files"
    - "Relevant AMA Q&A content is incorporated into Common Questions sections"
    - "Test suite validates content completeness and structure"
  artifacts:
    - path: "tests/test-knowledge-content.js"
      provides: "Validates all 10 topic files have >80 lines of real content"
      min_lines: 30
    - path: "tests/test-knowledge-structure.js"
      provides: "Validates consistent section structure across all topic files"
      min_lines: 30
    - path: "tests/test-content-completeness.js"
      provides: "Spot-checks key facts from index.html are present"
      min_lines: 40
    - path: "tests/test-ama-content.js"
      provides: "Validates Common Questions sections exist in topic files"
      min_lines: 20
    - path: "content/mcp.md"
      provides: "Full MCP content extracted from index.html and ama.html"
      min_lines: 150
      contains: "## Overview"
    - path: "content/skills.md"
      provides: "Full Skills content extracted from index.html and ama.html"
      min_lines: 120
      contains: "## Overview"
    - path: "content/plugins.md"
      provides: "Full Plugins content extracted from index.html and ama.html"
      min_lines: 100
      contains: "## Overview"
    - path: "content/hooks.md"
      provides: "Full Hooks content extracted from index.html and ama.html"
      min_lines: 100
      contains: "## Overview"
    - path: "content/agents.md"
      provides: "Full Agents content extracted from index.html and ama.html"
      min_lines: 120
      contains: "## Overview"
  key_links:
    - from: "content/mcp.md"
      to: "build/build.js"
      via: "frontmatter parsing and markdown rendering"
      pattern: "slug: mcp"
    - from: "tests/test-knowledge-content.js"
      to: "content/*.md"
      via: "file read and line count validation"
      pattern: "readFileSync.*content/"
---

<objective>
Create the test infrastructure for Phase 2 validation, then extract content from index.html and ama.html into the first 5 topic files (MCP, Skills, Plugins, Hooks, Agents) -- replacing placeholder content with real, structured knowledge.

Purpose: These 5 topics have the richest source material in index.html (dedicated JS detail objects, HTML sections, and AMA categories). Completing them first establishes the content pattern and provides test infrastructure for the remaining 5 topics.
Output: 4 test files + 5 fully-populated topic markdown files
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-base/02-RESEARCH.md

Source files to extract content FROM (read these thoroughly):
@index.html (root -- 2343 lines, contains HTML sections + JS details objects)
@ama.html (root -- 2816 lines, contains 48 Q&A entries across 11 categories)

Existing content files to replace (preserve frontmatter exactly):
@content/mcp.md
@content/skills.md
@content/plugins.md
@content/hooks.md
@content/agents.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test scaffolds for knowledge base validation</name>
  <files>tests/test-knowledge-content.js, tests/test-knowledge-structure.js, tests/test-content-completeness.js, tests/test-ama-content.js</files>
  <action>
Create 4 test files using Node.js built-in test runner (node:test) following the pattern in existing test files (tests/test-build.js, tests/test-nav.js).

**test-knowledge-content.js** -- For each of the 10 topic slugs (mcp, skills, plugins, hooks, agents, projects, connectors, commands, memory, settings):
- Read `content/{slug}.md`
- Assert file exists
- Assert line count > 80 (placeholders are ~35-43 lines)
- Assert file does NOT contain the string "Coming Soon" (placeholder marker)

**test-knowledge-structure.js** -- For each topic file:
- Parse YAML frontmatter (use gray-matter or regex for `---` delimited block)
- Assert frontmatter has required fields: title, slug, order, description, section
- Assert body contains `## Overview` heading
- Assert body contains at least 3 `##` level headings (indicating real structure)

**test-content-completeness.js** -- Spot-check key facts from index.html are present in content files:
- mcp.md contains "JSON-RPC" (protocol detail)
- mcp.md contains "stdio" (transport type)
- skills.md contains "frontmatter" (key concept)
- hooks.md contains at least 5 hook event names from: PreToolUse, PostToolUse, Notification, Stop, SubagentStop, PreCompact
- agents.md contains "subagent" or "sub-agent"
- plugins.md contains "manifest" or "bundle"

**test-ama-content.js** -- Assert:
- At least 5 of the 10 topic files contain a "## Common Questions" section
- Each Common Questions section contains at least one bold question (pattern: `**...**` or `###`)

All tests should use `describe()` and `it()` from node:test with `assert` from node:assert. Follow the existing test pattern in the project. Register these in package.json test script if needed (check current script pattern first).
  </action>
  <verify>
    <automated>node --test tests/test-knowledge-content.js tests/test-knowledge-structure.js tests/test-content-completeness.js tests/test-ama-content.js 2>&1 | head -50</automated>
  </verify>
  <done>All 4 test files exist and execute (tests will FAIL at this point because content is still placeholder -- that is expected and correct for Wave 0). No syntax errors in test files.</done>
</task>

<task type="auto">
  <name>Task 2: Extract content into MCP, Skills, Plugins, Hooks, and Agents markdown files</name>
  <files>content/mcp.md, content/skills.md, content/plugins.md, content/hooks.md, content/agents.md</files>
  <action>
Read index.html thoroughly (all 2343 lines) and ama.html thoroughly (all 2816 lines). Extract content into each file following the consistent section template from the research. PRESERVE EXISTING FRONTMATTER EXACTLY (title, slug, order, description, color, section) -- only replace the body content below the frontmatter closing `---`.

Use this section template for each file:

```
## Overview
[What it is, why it exists, which interfaces support it]

## How It Works
[General mechanics]

### In Claude Code
[Code-specific details, CLI commands, config]

### In Claude Chat
[Chat-specific details]

### In Claude Cowork
[Cowork-specific details]

## Configuration
[Setup steps, config files, schemas, code examples]

## Best Practices
[Patterns, tips, rules of thumb]

## Common Questions
[2-5 Q&A pairs extracted from ama.html]

## Related
[Links to related topics using relative paths like [Skills](skills.html)]
```

**Per-file extraction guide:**

**content/mcp.md** (TARGET: 200+ lines)
- Source: index.html `#stack` section (MCP layer), `details.mcp` JS object (lines ~2049-2078), `#extensions` MCP card, `#bestpractices` MCP items, `#reference` tables
- AMA source: category `mcp` (6 entries: mcp-01 through mcp-06)
- Key content: JSON-RPC 2.0 protocol, 3 transports (stdio, SSE, streamable HTTP), .mcp.json config format, CLI commands (`claude mcp add`), 4 scopes (local, project, user, managed), OAuth 2.0 auth, Tool Search, 50+ pre-built Chat connectors, MCP Apps extension, env var expansion
- IMPORTANT: "Connectors" in Chat context = MCP servers. Keep this in mcp.md, NOT in connectors.md

**content/skills.md** (TARGET: 150+ lines)
- Source: `details.skill` JS object, `#architecture` skill anatomy, `#stack` skills layer, `#extensions` skills card, `#reference` skill-related tables
- AMA source: category `skills` (4 entries: sk-01 through sk-04)
- Key content: YAML frontmatter spec (name, description, tags, globs, alwaysApply), progressive disclosure, partner skills, .claude/skills/ directory, skill vs hook vs agent comparison points

**content/plugins.md** (TARGET: 120+ lines)
- Source: `details.plugin` JS object, `#architecture` plugin anatomy, `#stack` plugins layer, `#extensions` plugins card
- AMA source: category `plugins` (2 entries: pl-01, pl-02), category `cowork` (cw-01 through cw-03 may reference plugins)
- Key content: What plugins bundle (skills, MCP servers, instructions), distribution model, install commands, plugin manifest, Cowork-specific behavior

**content/hooks.md** (TARGET: 150+ lines)
- Source: `details.hook` JS object, `#extensions` hooks card, `#bestpractices` hook items
- AMA source: category `hooks` (3 entries: hk-01 through hk-03)
- Key content: All hook events (PreToolUse, PostToolUse, Notification, Stop, SubagentStop, PreCompact etc -- list ALL from source), 4 hook types (bash, command list, match pattern, code review), config locations (.claude/hooks.json, settings.json), security model, example configs

**content/agents.md** (TARGET: 150+ lines)
- Source: `details.agent` JS object, `#extensions` agents card, `#reference` agent-related tables
- AMA source: category `workflows` (wf-01 through wf-04 may reference agents)
- Key content: Built-in agents, custom agent files, agent teams, subagent pattern, memory per agent, frontmatter fields, MCP server declarations in agents, `claude --agent` invocation

**Content extraction rules:**
1. Extract SEMANTIC content, not HTML structure. Convert HTML entities, strip tags, reformat as clean markdown.
2. Integrate AMA facts into the relevant sections (Overview, Configuration, etc.) where they add detail. Put genuinely FAQ-shaped content in "Common Questions" as Q&A pairs.
3. Use markdown code blocks with language hints (```json, ```bash, ```yaml) for all code examples.
4. Keep content factual and reference-quality. No filler prose.
5. Each file should have depth proportional to the source material available.
6. Run `npm run build` after writing each file to catch any markdown rendering issues early.
  </action>
  <verify>
    <automated>npm run build && node --test tests/test-knowledge-content.js tests/test-knowledge-structure.js tests/test-content-completeness.js</automated>
  </verify>
  <done>5 topic files contain real extracted content (no "Coming Soon"), each follows the consistent section template, build succeeds, content tests pass for these 5 files. Key facts from index.html are preserved: MCP mentions JSON-RPC and stdio, Skills mentions frontmatter, Hooks lists event names, Agents mentions subagents, Plugins mentions bundles/manifest.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- `node --test tests/test-knowledge-content.js` -- at least 5 topic files pass (the 5 written in this plan)
- `node --test tests/test-knowledge-structure.js` -- at least 5 topic files pass
- `node --test tests/test-content-completeness.js` -- all spot-checks for mcp, skills, plugins, hooks, agents pass
- No "Coming Soon" text in any of the 5 updated files
</verification>

<success_criteria>
- 4 test files created and executable
- 5 content files (mcp, skills, plugins, hooks, agents) contain real extracted content with consistent structure
- Build pipeline produces valid HTML for all updated files
- Content preserves key facts from source material
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-base/02-01-SUMMARY.md`
</output>
