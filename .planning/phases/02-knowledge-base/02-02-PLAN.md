---
phase: 02-knowledge-base
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - content/projects.md
  - content/connectors.md
  - content/commands.md
  - content/memory.md
  - content/settings.md
autonomous: true
requirements: [KNOW-01, KNOW-02, KNOW-03, KNOW-04]

must_haves:
  truths:
    - "Projects, Connectors, Commands, Memory, and Settings content files contain real extracted content (not placeholder)"
    - "Each topic file follows the same consistent section template as Plan 01 files"
    - "Connectors file is correctly about Interactive Apps (not MCP connections)"
    - "AMA content is incorporated into at least 5 total topic files across both plans"
    - "All 10 topic files build successfully and full test suite passes"
  artifacts:
    - path: "content/projects.md"
      provides: "Full Projects/CLAUDE.md content"
      min_lines: 120
      contains: "## Overview"
    - path: "content/connectors.md"
      provides: "Full Interactive Apps content (corrected from placeholder)"
      min_lines: 80
      contains: "## Overview"
    - path: "content/commands.md"
      provides: "Full Commands/Slash Commands content"
      min_lines: 80
      contains: "## Overview"
    - path: "content/memory.md"
      provides: "Full Memory System content"
      min_lines: 80
      contains: "## Overview"
    - path: "content/settings.md"
      provides: "Full Settings Hierarchy content"
      min_lines: 100
      contains: "## Overview"
  key_links:
    - from: "content/connectors.md"
      to: "index.html details.connector"
      via: "content extraction -- Interactive Apps, NOT MCP Connectors"
      pattern: "Interactive Apps"
    - from: "content/settings.md"
      to: "index.html #reference"
      via: "settings hierarchy and resolution order extraction"
      pattern: "resolution order|precedence"
---

<objective>
Extract content from index.html and ama.html into the remaining 5 topic files (Projects, Connectors, Commands, Memory, Settings) -- completing the full knowledge base. Correct the Connectors file to be about Interactive Apps (not MCP connections).

Purpose: Completes all 10 topic files, making the knowledge base fully populated. The Connectors file fix (Interactive Apps vs MCP Connectors confusion) is a critical correction identified in research.
Output: 5 fully-populated topic markdown files, full test suite green
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-base/02-RESEARCH.md
@.planning/phases/02-knowledge-base/02-01-SUMMARY.md

Source files to extract content FROM (read these thoroughly):
@index.html (root -- 2343 lines, contains HTML sections + JS details objects)
@ama.html (root -- 2816 lines, contains 48 Q&A entries across 11 categories)

Existing content files to replace (preserve frontmatter exactly):
@content/projects.md
@content/connectors.md
@content/commands.md
@content/memory.md
@content/settings.md

Reference for section template (follow the same pattern):
@content/mcp.md (completed in Plan 01 -- use as template reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract content into Projects, Connectors, Commands, Memory, and Settings files</name>
  <files>content/projects.md, content/connectors.md, content/commands.md, content/memory.md, content/settings.md</files>
  <action>
Read index.html thoroughly (all 2343 lines) and ama.html thoroughly (all 2816 lines). Also read the completed mcp.md from Plan 01 to match the section template and depth standard. Extract content into each file following the consistent section template. PRESERVE EXISTING FRONTMATTER EXACTLY -- only replace body content below the closing `---`.

Use the same section template as Plan 01:
```
## Overview
## How It Works
### In Claude Code
### In Claude Chat
### In Claude Cowork
## Configuration
## Best Practices
## Common Questions
## Related
```

Omit subsections that have no source content (e.g., if Commands has no Cowork-specific content, skip that subsection). Every file MUST have at minimum: Overview, How It Works (with at least one subsection), and Configuration or Best Practices.

**Per-file extraction guide:**

**content/projects.md** (TARGET: 150+ lines)
- Source: `details.project` JS object, `#extensions` projects card, `#bestpractices` project items, `#reference` CLAUDE.md tables
- AMA source: category `claude-code` (cc-01 through cc-06, especially CLAUDE.md related)
- Key content: CLAUDE.md file format, hierarchy (user/project/folder), project instructions, Chat projects, Cowork project instructions, loading order, best practices for writing CLAUDE.md, what to include vs exclude
- IMPORTANT: This covers both "Projects" as a concept AND CLAUDE.md as the configuration mechanism

**content/connectors.md** (TARGET: 100+ lines)
- Source: `details.connector` JS object, `#extensions` connector card
- AMA source: category `claude-chat` (ch-01 through ch-04 may reference), category `cowork` (cw-01 through cw-03)
- CRITICAL FIX: The current placeholder describes "Connectors" as database/service connections. This is WRONG. The `connector` extension point in index.html is actually "Interactive Apps" -- Figma, Slack, Asana, Canva embedded in Claude Chat conversations via MCP Apps extension. Update the content accordingly:
  - Title in frontmatter stays "Connectors" (don't change frontmatter)
  - But the `# heading` and content should clarify this is about Interactive Apps / MCP Apps
  - Explain the naming confusion: "Connectors" in Chat settings refers to MCP server connections (covered in mcp.md). The "Interactive Apps" extension point (this page) is about embedded UI applications within conversations.
  - Cover: available apps (Figma, Slack, Asana, Canva, etc.), how they work (sandboxed iframes), MCP Apps extension, Cowork connector controls

**content/commands.md** (TARGET: 80-120 lines)
- Source: index.html command-related content (may be limited), `#extensions` commands card if present
- AMA source: category `claude-code` (cc-* entries about slash commands)
- Key content: Slash commands in Claude Code, built-in commands (/help, /clear, /compact, /doctor, etc.), custom commands (if documented), command palette, keybindings
- NOTE: This topic may have less source material than others. Extract what exists. Shorter is fine if factual and complete.

**content/memory.md** (TARGET: 80-120 lines)
- Source: `#reference` memory section, agent memory references in `details.agent`
- AMA source: category `getting-started` (gs-* entries about memory)
- Key content: CLAUDE.md as memory, session memory, memory commands, how Claude remembers across sessions, memory file locations, agent-specific memory, what persists vs what doesn't

**content/settings.md** (TARGET: 120+ lines)
- Source: `#reference` settings tables, `#bestpractices` settings items
- AMA source: category `security` (sec-01 through sec-05 may cover settings/permissions), category `claude-code` (cc-* settings related)
- Key content: Settings hierarchy and resolution order (enterprise > organization > user > project), settings.json locations, permission model (allowedTools, blockedTools), environment variables, API key configuration, model selection, all settings scopes and their precedence

**Content extraction rules (same as Plan 01):**
1. Extract SEMANTIC content, not HTML structure. Convert HTML entities, strip tags, reformat as clean markdown.
2. Integrate AMA facts into relevant sections. Put FAQ-shaped content in "Common Questions" (2-5 per topic).
3. Use markdown code blocks with language hints for all code examples.
4. Keep content factual and reference-quality.
5. Run `npm run build` after writing each file to catch rendering issues.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>All 5 remaining topic files contain real extracted content (no "Coming Soon"), each follows the consistent section template, connectors.md correctly describes Interactive Apps (not MCP connections), build succeeds, and the FULL test suite passes (all 10 topic files validated). At least 5 topic files across both plans have "Common Questions" sections with AMA-sourced content.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- `npm test` -- full suite passes including all 4 knowledge base test files
- `node --test tests/test-knowledge-content.js` -- all 10 topic files pass (>80 lines, no "Coming Soon")
- `node --test tests/test-knowledge-structure.js` -- all 10 topic files have consistent structure
- `node --test tests/test-content-completeness.js` -- all spot-checks pass
- `node --test tests/test-ama-content.js` -- at least 5 files have Common Questions sections
- connectors.md mentions "Interactive Apps" and does NOT describe generic database/service connections
</verification>

<success_criteria>
- 5 content files (projects, connectors, commands, memory, settings) contain real extracted content
- Connectors naming confusion is resolved (content is about Interactive Apps)
- All 10 topic files pass the full validation suite
- Build pipeline produces valid HTML for all content
- Phase 2 knowledge base is complete -- all placeholder content replaced
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-base/02-02-SUMMARY.md`
</output>
