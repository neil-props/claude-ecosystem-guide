---
phase: 06-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-agent.js
  - .claude/agents/claude-ecosystem.md
  - .claude/skills/ecosystem-mcp/SKILL.md
  - .claude/skills/ecosystem-skills/SKILL.md
  - .claude/skills/ecosystem-plugins/SKILL.md
  - .claude/skills/ecosystem-hooks/SKILL.md
  - .claude/skills/ecosystem-agents/SKILL.md
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - AGNT-04
  - AGNT-05
  - AGNT-06
  - AGNT-07
  - AGNT-08
  - AGNT-09

must_haves:
  truths:
    - "Agent file exists at .claude/agents/claude-ecosystem.md with valid YAML frontmatter"
    - "Agent system prompt covers all 9 AGNT requirements (knowledge, citations, context reading, workflows, audit, comparisons)"
    - "First 5 skill files exist with condensed knowledge (~100-150 lines each)"
    - "Test file validates agent structure, frontmatter, skill existence, and system prompt content"
  artifacts:
    - path: ".claude/agents/claude-ecosystem.md"
      provides: "Agent definition with system prompt"
      min_lines: 200
    - path: "tests/test-agent.js"
      provides: "Validation tests for all AGNT requirements"
      min_lines: 80
    - path: ".claude/skills/ecosystem-mcp/SKILL.md"
      provides: "Condensed MCP knowledge"
      min_lines: 80
    - path: ".claude/skills/ecosystem-skills/SKILL.md"
      provides: "Condensed Skills knowledge"
      min_lines: 80
    - path: ".claude/skills/ecosystem-plugins/SKILL.md"
      provides: "Condensed Plugins knowledge"
      min_lines: 60
    - path: ".claude/skills/ecosystem-hooks/SKILL.md"
      provides: "Condensed Hooks knowledge"
      min_lines: 80
    - path: ".claude/skills/ecosystem-agents/SKILL.md"
      provides: "Condensed Agents knowledge"
      min_lines: 80
  key_links:
    - from: ".claude/agents/claude-ecosystem.md"
      to: ".claude/skills/ecosystem-*"
      via: "skills: frontmatter field"
      pattern: "skills:"
    - from: "tests/test-agent.js"
      to: ".claude/agents/claude-ecosystem.md"
      via: "fs.readFileSync validation"
      pattern: "readFileSync.*agents.*claude-ecosystem"
---

<objective>
Create the Claude Ecosystem agent file, test scaffold, and first 5 condensed skill files (MCP, Skills, Plugins, Hooks, Agents).

Purpose: Establish the complete agent definition with all system prompt behavior (citations, context reading, guided workflows, audit, comparisons) and half the knowledge base. Tests validate all AGNT requirements structurally.

Output: Agent file, test file, 5 skill files
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent/06-RESEARCH.md
@.planning/phases/06-agent/06-VALIDATION.md

Source content for skill condensation:
@content/mcp.md
@content/skills.md
@content/plugins.md
@content/hooks.md
@content/agents.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffold and agent file with complete system prompt</name>
  <files>tests/test-agent.js, .claude/agents/claude-ecosystem.md</files>
  <action>
**Step 1: Create `tests/test-agent.js`** with tests covering all AGNT requirements using the existing Node.js test runner pattern (see `tests/test-build.js` for convention). Use `gray-matter` for YAML parsing (already a project dependency). Tests to include:

- **AGNT-01**: Agent file exists at `.claude/agents/claude-ecosystem.md`, has valid YAML frontmatter with `name: claude-ecosystem`
- **AGNT-02**: Frontmatter has `skills:` array listing all 10 skill names (`ecosystem-mcp`, `ecosystem-skills`, `ecosystem-plugins`, `ecosystem-hooks`, `ecosystem-agents`, `ecosystem-projects`, `ecosystem-connectors`, `ecosystem-commands`, `ecosystem-memory`, `ecosystem-settings`)
- **AGNT-03**: All 10 skill directories exist under `.claude/skills/` with a `SKILL.md` file inside each
- **AGNT-04**: Frontmatter `tools:` includes `WebFetch` and `WebSearch`
- **AGNT-05**: System prompt body contains citation instructions (look for "cite" or "source" pattern)
- **AGNT-06**: System prompt contains project context reading instructions (look for "CLAUDE.md" and ".mcp.json" and "settings")
- **AGNT-07**: System prompt contains setup workflow instructions (look for "setup" and "workflow" or "step-by-step")
- **AGNT-08**: System prompt contains audit/health-check instructions (look for "audit" or "health check")
- **AGNT-09**: System prompt contains comparison format template (look for "Dimension" table pattern or "vs" comparison structure)

Use `path.join(process.cwd(), ...)` for file paths. Group related assertions in `describe` blocks.

**Step 2: Create `.claude/agents/claude-ecosystem.md`** with:

**Frontmatter (YAML):**
```yaml
name: claude-ecosystem
description: Expert guide to the Claude Code ecosystem -- MCP, Skills, Plugins, Hooks, Agents, Projects, Connectors, Commands, Memory, and Settings
tools: Read, Grep, Glob, Bash, WebFetch, WebSearch
model: sonnet
maxTurns: 50
skills:
  - ecosystem-mcp
  - ecosystem-skills
  - ecosystem-plugins
  - ecosystem-hooks
  - ecosystem-agents
  - ecosystem-projects
  - ecosystem-connectors
  - ecosystem-commands
  - ecosystem-memory
  - ecosystem-settings
```

**Body (system prompt, ~200-300 lines):** Structure as follows:

1. **Identity section**: "You are the Claude Ecosystem Guide -- an expert on the Claude Code extension system..." Explain the 10 extension points briefly.

2. **Knowledge Sources (Priority Order)** (supports AGNT-04):
   - Preloaded skills (primary -- use FIRST)
   - Local content files -- `Read` from `content/*.md` for detailed reference
   - Official docs -- `WebFetch` from `https://docs.anthropic.com/en/docs/claude-code/` for latest info
   - Web search -- `WebSearch` as last resort
   - Always state which source you used

3. **Citation Instructions** (supports AGNT-05): "When answering, cite your source: [From preloaded knowledge], [From content/topic.md], [From official docs: URL], or [From web search]. Point users to the relevant page on the ecosystem guide site."

4. **Reading the User's Project Context** (supports AGNT-06): Step-by-step instructions to:
   - Read `CLAUDE.md` in project root and `.claude/CLAUDE.md`
   - Read `.mcp.json` for MCP server config
   - Read `.claude/settings.json` and `.claude/settings.local.json`
   - List `.claude/skills/*/SKILL.md`, `.claude/agents/*.md`, `.claude/commands/*.md`
   - Provide contextual recommendations based on findings

5. **Guided Setup Workflows** (supports AGNT-07): Templates for:
   - Creating a new skill (step-by-step: create dir, write SKILL.md with frontmatter, test invocation)
   - Setting up an MCP server (choose transport, create config, verify connection)
   - Creating a hook (choose lifecycle event, write hook config, test)
   - Setting up a custom agent (create agent file, configure tools/skills, test with --agent)
   - Configuring project settings (create CLAUDE.md, settings hierarchy, permission rules)

6. **Ecosystem Health Check / Audit** (supports AGNT-08): Checklist the agent runs through:
   - Does CLAUDE.md exist? Is it well-structured?
   - Are MCP servers configured? Any connection issues?
   - Are there custom skills? Do they have valid frontmatter?
   - Settings hierarchy -- any conflicts between project/local/user?
   - Hooks configured? Any deprecated patterns?
   - Commands directory present? Should migrate to skills?
   - Overall score and improvement suggestions

7. **Comparison Questions** (supports AGNT-09): Format template:
   ```
   ### [X] vs [Y]
   | Dimension | [X] | [Y] |
   |-----------|-----|-----|
   | Purpose | ... | ... |
   | Scope | ... | ... |
   | Invocation | ... | ... |
   | Best for | ... | ... |

   **Use [X] when:** ...
   **Use [Y] when:** ...
   **Use both together when:** ...
   ```
   Include common comparisons: Skill vs Agent, MCP vs Plugin, Hook vs Skill, Command vs Skill.

8. **Response Guidelines**: Be concise, use code examples, format with markdown tables when comparing, always offer next steps.
  </action>
  <verify>
    <automated>node --test tests/test-agent.js 2>&1 | head -30</automated>
  </verify>
  <done>Agent file exists with valid frontmatter (name, tools, skills, model, maxTurns). Tests exist covering all 9 AGNT requirements. Tests for AGNT-03 will fail (skill files not yet created) -- that is expected at this stage.</done>
</task>

<task type="auto">
  <name>Task 2: Create first 5 condensed skill files (MCP, Skills, Plugins, Hooks, Agents)</name>
  <files>.claude/skills/ecosystem-mcp/SKILL.md, .claude/skills/ecosystem-skills/SKILL.md, .claude/skills/ecosystem-plugins/SKILL.md, .claude/skills/ecosystem-hooks/SKILL.md, .claude/skills/ecosystem-agents/SKILL.md</files>
  <action>
Create 5 condensed skill files. Each skill file is a SKILL.md inside its own directory under `.claude/skills/`.

**Skill file format** (each file):
```yaml
---
name: ecosystem-{topic}
description: Knowledge about {Topic Name} in the Claude Code ecosystem
user-invocable: false
disable-model-invocation: true
---
```

Body is condensed knowledge (~100-150 lines) extracted from the corresponding `content/{topic}.md` file. **Strip all HTML** (tab markup, div wrappers, button elements). Convert to pure markdown. Focus on:
- Overview / what it is
- How it works (key mechanics)
- Configuration essentials (most common settings)
- Key decision guidance (when to use, when not to)
- Common pitfalls (2-3 most important)

Do NOT include: Full reference specs (too long), recipe content, HTML tab structure, redundant examples.

**Per-skill specifics:**

1. **ecosystem-mcp** (from `content/mcp.md`, 689 lines -> ~120 lines): Cover 3 transport types, .mcp.json config basics, tool/resource/prompt concepts, stdio vs SSE decision, auth patterns.

2. **ecosystem-skills** (from `content/skills.md`, 395 lines -> ~100 lines): Cover SKILL.md format, frontmatter fields, progressive disclosure, skill discovery, when to use skills vs other points.

3. **ecosystem-plugins** (from `content/plugins.md`, 451 lines -> ~100 lines): Cover plugin = bundle of skills, manifest format, distribution, when plugins vs standalone skills.

4. **ecosystem-hooks** (from `content/hooks.md`, 695 lines -> ~120 lines): Cover lifecycle events (PreToolUse, PostToolUse, Notification, Stop, SubagentStop), hook config in settings.json, command execution, matcher patterns, common use cases.

5. **ecosystem-agents** (from `content/agents.md`, 524 lines -> ~110 lines): Cover agent file format, frontmatter fields (tools, model, skills, maxTurns, memory), --agent flag invocation, Agent tool for subagents, when agents vs skills.
  </action>
  <verify>
    <automated>node --test tests/test-agent.js 2>&1 | head -30</automated>
  </verify>
  <done>5 skill files exist at correct paths with valid SKILL.md frontmatter. Each contains condensed pure-markdown knowledge (no HTML). Combined with Plan 02's 5 skills, AGNT-03 test will pass.</done>
</task>

</tasks>

<verification>
- `node --test tests/test-agent.js` -- most tests pass (AGNT-03 may partially fail until Plan 02 creates remaining 5 skills)
- Agent file has valid YAML frontmatter parseable by gray-matter
- Each skill file has valid YAML frontmatter with name and description
- No HTML markup in skill files (pure markdown only)
</verification>

<success_criteria>
- Agent file at `.claude/agents/claude-ecosystem.md` with complete system prompt (~200-300 lines)
- Test file at `tests/test-agent.js` covering all 9 AGNT requirements
- 5 condensed skill files in `.claude/skills/ecosystem-{topic}/SKILL.md`
- All non-AGNT-03 tests pass (AGNT-03 needs Plan 02's remaining 5 skills)
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent/06-01-SUMMARY.md`
</output>
