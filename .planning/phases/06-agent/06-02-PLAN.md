---
phase: 06-agent
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .claude/skills/ecosystem-projects/SKILL.md
  - .claude/skills/ecosystem-connectors/SKILL.md
  - .claude/skills/ecosystem-commands/SKILL.md
  - .claude/skills/ecosystem-memory/SKILL.md
  - .claude/skills/ecosystem-settings/SKILL.md
  - package.json
autonomous: false
requirements:
  - AGNT-03

must_haves:
  truths:
    - "All 10 skill files exist under .claude/skills/ with valid SKILL.md frontmatter"
    - "Agent answers ecosystem questions using preloaded skill knowledge and cites sources"
    - "Agent can be invoked with claude --agent claude-ecosystem and answers questions"
  artifacts:
    - path: ".claude/skills/ecosystem-projects/SKILL.md"
      provides: "Condensed Projects/CLAUDE.md knowledge"
      min_lines: 80
    - path: ".claude/skills/ecosystem-connectors/SKILL.md"
      provides: "Condensed Connectors knowledge"
      min_lines: 40
    - path: ".claude/skills/ecosystem-commands/SKILL.md"
      provides: "Condensed Commands knowledge"
      min_lines: 40
    - path: ".claude/skills/ecosystem-memory/SKILL.md"
      provides: "Condensed Memory knowledge"
      min_lines: 60
    - path: ".claude/skills/ecosystem-settings/SKILL.md"
      provides: "Condensed Settings knowledge"
      min_lines: 80
  key_links:
    - from: ".claude/agents/claude-ecosystem.md"
      to: ".claude/skills/ecosystem-projects"
      via: "skills: frontmatter preloading"
      pattern: "ecosystem-projects"
    - from: "tests/test-agent.js"
      to: ".claude/skills/ecosystem-*"
      via: "fs.existsSync validation"
      pattern: "ecosystem-(projects|connectors|commands|memory|settings)"
---

<objective>
Create remaining 5 condensed skill files (Projects, Connectors, Commands, Memory, Settings) and verify the complete agent setup works.

Purpose: Complete the full 10-skill knowledge base so all AGNT-03 tests pass and the agent has comprehensive coverage of every extension point. Human verification confirms the agent works interactively.

Output: 5 skill files, all tests green, verified agent interaction
</objective>

<execution_context>
@/Users/sharode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent/06-RESEARCH.md
@.planning/phases/06-agent/06-01-SUMMARY.md

Source content for skill condensation:
@content/projects.md
@content/connectors.md
@content/commands.md
@content/memory.md
@content/settings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create remaining 5 condensed skill files and register agent tests in npm</name>
  <files>.claude/skills/ecosystem-projects/SKILL.md, .claude/skills/ecosystem-connectors/SKILL.md, .claude/skills/ecosystem-commands/SKILL.md, .claude/skills/ecosystem-memory/SKILL.md, .claude/skills/ecosystem-settings/SKILL.md, package.json</files>
  <action>
**Step 1: Create 5 condensed skill files** following the same format as Plan 01's skills. Each file is `.claude/skills/ecosystem-{topic}/SKILL.md`.

**Skill file format** (each file):
```yaml
---
name: ecosystem-{topic}
description: Knowledge about {Topic Name} in the Claude Code ecosystem
user-invocable: false
disable-model-invocation: true
---
```

Body is condensed knowledge (~60-120 lines) extracted from `content/{topic}.md`. Strip all HTML, convert to pure markdown. Focus on overview, how it works, configuration essentials, decision guidance, common pitfalls.

**Per-skill specifics:**

1. **ecosystem-projects** (from `content/projects.md`, 440 lines -> ~100 lines): Cover CLAUDE.md hierarchy (project root, .claude/CLAUDE.md, user-level ~/.claude/CLAUDE.md), loading order, what to include in CLAUDE.md, project memory, team vs personal instructions.

2. **ecosystem-connectors** (from `content/connectors.md`, 146 lines -> ~50 lines): Cover what connectors are (interactive apps in Claude Chat -- Figma, Slack, Asana, etc.), how they differ from MCP in Code, available connectors list, when to use connectors vs MCP servers.

3. **ecosystem-commands** (from `content/commands.md`, 162 lines -> ~50 lines): Cover legacy `.claude/commands/` system, transition to skills, slash command syntax, when commands still make sense, migration path to skills.

4. **ecosystem-memory** (from `content/memory.md`, 369 lines -> ~80 lines): Cover 4-layer memory architecture (CLAUDE.md instructions, auto-memory ~/.claude/memory, /memory command, agent memory field), persistence behavior, what gets remembered vs forgotten, memory management.

5. **ecosystem-settings** (from `content/settings.md`, 502 lines -> ~100 lines): Cover 5-level settings hierarchy (Managed > Enterprise > CLI flags > settings.local.json > settings.json > user), resolution order, common settings (permissions, model, hooks), permission patterns (allow/deny tools, MCP scopes).

**Step 2: Update `package.json`** to add `tests/test-agent.js` to the test script. Change the `test` script from:
```
"test": "node --test tests/test-build.js tests/test-anchors.js tests/test-highlighting.js tests/test-nav.js"
```
to:
```
"test": "node --test tests/test-build.js tests/test-anchors.js tests/test-highlighting.js tests/test-nav.js tests/test-agent.js"
```
  </action>
  <verify>
    <automated>node --test tests/test-agent.js</automated>
  </verify>
  <done>All 10 skill files exist. All tests in test-agent.js pass (AGNT-01 through AGNT-09). No HTML in any skill file. package.json test script includes tests/test-agent.js.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify agent works interactively</name>
  <files>none</files>
  <action>Human verifies the complete agent works by invoking it and testing key behaviors.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Human confirms agent answers questions, handles comparisons, reads local context, and cites sources. Full test suite passes including agent tests.</done>
  <what-built>Complete Claude Ecosystem agent with 10 preloaded skill files covering all extension points. The agent answers ecosystem questions, cites sources, reads local project context, provides guided workflows, performs setup audits, and handles comparison questions.</what-built>
  <how-to-verify>
    1. Run all tests: `npm test` -- all should pass (now includes test-agent.js)
    2. Invoke the agent: `claude --agent claude-ecosystem`
    3. Test knowledge: Ask "What is MCP and when should I use it?"
    4. Test comparison: Ask "What's the difference between skills and agents?"
    5. Test audit: Ask "Can you audit my Claude Code setup?"
    6. Test context: Ask "What does my CLAUDE.md say?" (should read local files)
    7. Verify citations: Agent should indicate where information comes from
    8. Exit agent session
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `node --test tests/test-agent.js` -- all tests pass
- `npm test` -- full suite green (includes test-agent.js)
- All 10 `.claude/skills/ecosystem-*/SKILL.md` files exist with valid frontmatter
- Agent responds correctly when invoked with `claude --agent claude-ecosystem`
</verification>

<success_criteria>
- All 10 condensed skill files complete (AGNT-03 fully satisfied)
- All automated tests pass (AGNT-01 through AGNT-09 structurally verified)
- npm test script includes tests/test-agent.js for full suite coverage
- Human confirms agent works interactively (knowledge, comparisons, audit, context reading)
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent/06-02-SUMMARY.md`
</output>